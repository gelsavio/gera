diff --git a/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.25-etapa-8E-bateria/index.html b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/index.html
index 4cbe6dd..6f36a74 100644
--- a/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.25-etapa-8E-bateria/index.html
+++ b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/index.html
@@ -15,7 +15,7 @@
 <link rel="icon" type="image/png" sizes="512x512" href="./icons/icon-512.png">
 <link rel="apple-touch-icon" href="./icons/icon-192.png">
 <meta name="screen-orientation" content="landscape">
-<title>GERA v3.15.25</title>
+<title>GERA v3.15.26</title>
 <script src="./js/storage.js"></script>
 <script>
 (function(){
@@ -38,7 +38,7 @@
 <div id="rotate-msg"><div class="icon">📱</div><h2>Gire o dispositivo</h2><p>O GERA foi organizado para uso horizontal.</p></div>
 <header class="app-header">
   <div class="header-brand">
-    <h1><span class="gera-brand-mark" aria-hidden="true"><img src="icons/icon-192.png" alt=""></span><span class="gera-title">GERA</span><span class="gera-subtitle">Gerador de Acompanhamentos</span><small class="app-version">v3.15.25</small></h1>
+    <h1><span class="gera-brand-mark" aria-hidden="true"><img src="icons/icon-192.png" alt=""></span><span class="gera-title">GERA</span><span class="gera-subtitle">Gerador de Acompanhamentos</span><small class="app-version">v3.15.26</small></h1>
   </div>
   <div class="header-actions">
     <button type="button" id="theme-cycle" class="secondary compact-theme-button" aria-label="Alterar tema do aplicativo" title="Alterar tema">🎨 Escuro</button>
@@ -163,7 +163,7 @@
   <div class="redesign-brandmark">
     <span class="redesign-logo">GERA</span>
     <span class="redesign-tagline">Gerador de Acompanhamentos</span>
-    <small>v3.15.25</small>
+    <small>v3.15.26</small>
   </div>
   <button class="redesign-song-pill" id="redesign-song-pill" type="button" title="Escolher música">♫ <span id="redesign-song-name">Nenhuma música</span></button>
   <div id="redesign-header-text-preview" class="sequence-text-preview sequence-text-preview-floating" aria-live="polite">
@@ -819,6 +819,7 @@
 <script src="./js/ui/keyboard.js"></script>
 <script src="./js/ui/chords-circle.js"></script>
 <script src="./js/ui/drums.js"></script>
+<script src="./js/ui/sequencer.js"></script>
 <script src="./js/audio/core.js"></script>
 <script>
 "use strict";
@@ -3617,18 +3618,10 @@ function isFinalAutoEndPass(section){
  return target>0&&currentSectionRepetition>=target;
 }
 function syncSequenceRepeatInputs(){
- document.querySelectorAll('[data-section-repeat-display]').forEach(function(display){
-  const section=display.dataset.sectionRepeatDisplay;
-  display.textContent=String(sectionRepeatValue(section));
-  display.title=sectionRepeatValue(section)===0?'Fora do modo Auto':sectionRepeatValue(section)+' repetições';
- });
+ sequencerUI.syncRepeatInputs(sectionRepeatValue);
 }
 function syncSequenceHoldLoopButton(){
- const button=$('sequence-hold-loop');
- if(!button)return;
- button.classList.toggle('active',sequenceHoldLoop);
- button.textContent=sequenceHoldLoop?'🔁 Em Loop':'🔁 Pôr em Loop';
- button.title=sequenceHoldLoop?'Clique para liberar a progressão':'Manter a seção atual em repetição';
+ sequencerUI.syncHoldLoop(sequenceHoldLoop);
 }
 function toggleSequenceHoldLoop(){
  if(!sequencePlaying){
@@ -3643,26 +3636,9 @@ function toggleSequenceHoldLoop(){
  else setStatus('Progressão liberada; seguirá ao final da repetição atual');
 }
 function syncSequenceAutoButton(){
- const loopButton=$('sequence-auto');
- const endButton=$('sequence-auto-end');
-
- if(loopButton){
-  loopButton.classList.toggle('active',sequenceAuto);
-  loopButton.textContent=sequenceAuto?'Auto ✓':'Auto';
-  loopButton.title=sequenceAuto
-   ?'Auto contínuo ligado: ao final volta para a primeira sequência'
-   :'Ligar execução automática contínua';
- }
-
- if(endButton){
-  endButton.classList.toggle('active',sequenceAutoEnd);
-  endButton.textContent=sequenceAutoEnd?'Auto Fim ✓':'Auto Fim';
-  endButton.title=sequenceAutoEnd
-   ?'Auto Fim ligado: executa uma vez e encerra na última sequência'
-   :'Executar a música uma vez e encerrar com virada de bateria';
- }
+ sequencerUI.syncAuto(sequenceAuto,sequenceAutoEnd);
 }
-function syncSequencePanelVisibility(){const panel=$('sequence-panel'),button=$('sequence-toggle');if(!panel||!button)return;panel.classList.toggle('sequence-hidden',!sequencePanelVisible);button.classList.toggle('sequence-toggle-active',sequencePanelVisible);button.title=sequencePanelVisible?'Ocultar barra de sequências':'Mostrar barra de sequências';}
+function syncSequencePanelVisibility(){sequencerUI.syncPanelVisibility(sequencePanelVisible)}
 function normalizedSequenceOrder(order){
  const keys=Object.keys(SEQUENCE_SECTION_LABELS);
  const source=Array.isArray(order)?order:[];
@@ -3773,21 +3749,18 @@ async function resetSequenceOrder(){
 }
 function syncSequenceSectionButtons(){
  syncSequenceOrderDom();
- document.querySelectorAll('[data-sequence-section]').forEach(b=>{
-  const key=b.dataset.sequenceSection;
-  b.classList.toggle('active-section',key===activeSequenceSection);
-  b.classList.toggle('queued-section',sequencePlaying&&key===queuedSequenceSection);
-  b.classList.toggle('has-content',(sequenceSections[key]||[]).length>0);
-  b.setAttribute('aria-label',SEQUENCE_SECTION_LABELS[key]+(sequencePlaying&&key===queuedSequenceSection?' — próxima seção':''));
- });
- document.querySelectorAll('[data-section-control]').forEach(function(control){
-  control.classList.toggle('playing-repeat',sequencePlaying&&control.dataset.sectionControl===activeSequenceSection);
+ sequencerUI.syncSectionButtons({
+  labels:SEQUENCE_SECTION_LABELS,
+  activeSection:activeSequenceSection,
+  queuedSection:queuedSequenceSection,
+  playing:sequencePlaying,
+  hasContent:function(section){return(sequenceSections[section]||[]).length>0}
  });
  syncSequenceRepeatInputs();
  syncSequenceAutoButton();
  syncSectionDrumControls();
 }
-function syncSequenceRecordButton(){const b=$('sequence-record');if(!b)return;b.classList.toggle('sequence-recording',sequenceRecording);b.textContent=sequenceRecording?'■ Gravando':'● Gravar'}
+function syncSequenceRecordButton(){sequencerUI.syncRecordButton(sequenceRecording)}
 function clearChordLatchState(releaseTime=180){clearAccompanimentSchedule();latchedRoot=null;latchedType=null;latchedButton=null;pendingRoot=null;pendingType=null;pendingButton=null;document.querySelectorAll('.chord.latched,.chord.queued').forEach(b=>b.classList.remove('latched','queued'));if(releaseTime>0)releaseAll(releaseTime);syncInstrumentChangeLock();maybeStopMasterTransport()}
 function selectSequenceSection(section){
  resetSequenceDeleteArm();
@@ -5005,19 +4978,7 @@ function toggleStopSequenceAtEnd(){
 }
 function syncSequencePlayButton(){
  if(compactModeActive)setTimeout(syncCompactControls,0);
- const button=$('sequence-play');
- if(!button)return;
- button.classList.toggle('sequence-play-active',sequencePlaying);
- button.classList.remove('sequence-stop-pending');
- if(sequencePlaying){
-  button.textContent='⏹ Parar Sequência';
-  button.title='Parar imediatamente a sequência em execução';
-  button.setAttribute('aria-pressed','true');
- }else{
-  button.textContent='▶ Tocar Sequência';
-  button.title='Reproduzir a sequência selecionada';
-  button.setAttribute('aria-pressed','false');
- }
+ sequencerUI.syncPlayButton(sequencePlaying);
 }
 function toggleSequencePlayback(){
  if(sequencePlaying){
@@ -5030,14 +4991,8 @@ function toggleSequencePlayback(){
 }
 function syncSequenceDrumButton(){
  if(compactModeActive)setTimeout(syncCompactControls,0);
- const button=$('sequence-stop-drums');
- if(!button)return;
  const active=drumRunning||drumStartQueued;
- button.classList.toggle('sequence-drums-active',active);
- button.textContent='🥁';
- button.setAttribute('aria-label',active?'Parar bateria':'Iniciar bateria');
- button.setAttribute('aria-pressed',String(active));
- button.title=active?'Parar somente a bateria':'Iniciar a bateria configurada para a sequência selecionada';
+ sequencerUI.syncDrumButton(active);
 }
 function toggleSequenceDrums(){
  if(drumRunning||drumStartQueued){stopSequenceDrumsOnly();syncSequenceDrumButton();return}
@@ -6561,23 +6516,6 @@ $('change-quarter').onclick=function(){selectChordChangeMode('quarterBar')};
 $('change-half').onclick=function(){selectChordChangeMode('halfBar')};
 $('change-three-quarter').onclick=function(){selectChordChangeMode('threeQuarterBar')};
 $('change-full').onclick=function(){selectChordChangeMode('nextBar')};
-document.querySelectorAll('[data-sequence-section]').forEach(function(button){
- button.onclick=function(){selectSequenceSection(button.dataset.sequenceSection)};
-});
-document.querySelectorAll('[data-repeat-adjust]').forEach(function(button){
- button.onclick=function(){
-  const section=button.dataset.repeatAdjust;
-  const delta=Number(button.dataset.delta);
-  let value=sectionRepeatValue(section)+delta;
-  if(value<0)value=0;
-  if(value>MAX_SEQUENCE_REPEATS)value=MAX_SEQUENCE_REPEATS;
-  sequenceRepeats[section]=value;
-  saveChordSequence();
-  syncSequenceSectionButtons();
-  setStatus(SEQUENCE_SECTION_LABELS[section]+(value===0?' ficará fora do modo Auto':' será repetida '+value+(value===1?' vez':' vezes')+' antes de avançar'));
- };
-});
-$('sequence-record').onclick=()=>{if(sequencePlaying)stopChordSequence('Sequência parada para iniciar a gravação');sequenceRecording=!sequenceRecording;syncSequenceRecordButton();syncSequenceRepeatInputs();syncSequenceAutoButton();setStatus(sequenceRecording?'Gravando '+SEQUENCE_SECTION_LABELS[activeSequenceSection]+': toque em acordes ou notas do teclado e use as pausas quando quiser':'Gravação encerrada')};
 function chordChangeModeFraction(mode){
  if(mode==='eighthBar')return .125;
  if(mode==='quarterBar')return .25;
@@ -6595,44 +6533,46 @@ function chordChangeModeFractionLabel(mode){
  if(fraction===1)return '1';
  return '0';
 }
+function adjustSequenceRepeat(section,delta){
+ let value=sectionRepeatValue(section)+delta;
+ if(value<0)value=0;
+ if(value>MAX_SEQUENCE_REPEATS)value=MAX_SEQUENCE_REPEATS;
+ sequenceRepeats[section]=value;
+ saveChordSequence();
+ syncSequenceSectionButtons();
+ setStatus(SEQUENCE_SECTION_LABELS[section]+(value===0?' ficará fora do modo Auto':' será repetida '+value+(value===1?' vez':' vezes')+' antes de avançar'));
+}
 function syncSequencePauseButton(){
- const button=$('sequence-rest');
- if(!button)return;
  const fraction=chordChangeModeFraction(chordChangeMode);
- button.textContent='⏸ Pausa '+chordChangeModeFractionLabel(chordChangeMode);
- button.disabled=fraction===0;
- button.title=fraction===0?'Selecione um tempo de ⅛ a 1 para adicionar uma pausa':'Adicionar pausa de '+durationLabelForStatus(fraction);
+ sequencerUI.syncPauseButton({
+  fraction:fraction,
+  fractionLabel:chordChangeModeFractionLabel(chordChangeMode),
+  durationLabel:durationLabelForStatus(fraction)
+ });
 }
-$('sequence-rest').onclick=function(){
+function addSelectedSequencePause(){
  const fraction=chordChangeModeFraction(chordChangeMode);
  if(fraction===0){setStatus('Selecione um tempo de ⅛ a 1 antes de adicionar a pausa');return}
  addPauseToSequence(fraction);
-};
-$('sequence-play').onclick=function(){
+}
+function toggleSelectedSequencePlayback(){
  if(!sequencePlaying){sequenceHoldLoop=false;syncSequenceHoldLoopButton()}
  toggleSequencePlayback();
-};
-$('sequence-hold-loop').onclick=toggleSequenceHoldLoop;
-$('section-instrument').onchange=saveActiveSectionDrumControls;
-$('section-next').onchange=saveActiveSectionDrumControls;
-$('section-drum-pattern').onchange=function(){
+}
+function changeActiveSectionDrumPattern(value){
  const oldConfig=normalizeSectionDrum(sequenceDrums[activeSequenceSection]);
- const selected=this.value||null;
+ const selected=value||null;
  oldConfig.padrao=selected;
  oldConfig.drumSource='library';
  oldConfig.drumData=null;
  sequenceDrums[activeSequenceSection]=normalizeSectionDrum(oldConfig);
  saveChordSequence();
  syncSectionDrumControls();
-};
-$('section-drum-entry').onchange=saveActiveSectionDrumControls;
-$('section-drum-exit').onchange=saveActiveSectionDrumControls;
-$('section-drum-final').onchange=saveActiveSectionDrumControls;
+}
 $('song-export').onclick=exportCurrentSong;
 $('song-import').onclick=chooseSongImportFile;
 $('song-import-file').onchange=handleSongImportFile;
-$('sequence-stop-drums').onclick=toggleSequenceDrums;
-$('sequence-auto').onclick=function(){
+function toggleSequenceAuto(){
  sequenceAuto=!sequenceAuto;
  if(sequenceAuto)sequenceAutoEnd=false;
  currentSectionRepetition=1;
@@ -6651,9 +6591,9 @@ $('sequence-auto').onclick=function(){
  }else{
   setStatus('Auto contínuo desligado');
  }
-};
+}
 
-$('sequence-auto-end').onclick=function(){
+function toggleSequenceAutoEnd(){
  sequenceAutoEnd=!sequenceAutoEnd;
  if(sequenceAutoEnd)sequenceAuto=false;
  currentSectionRepetition=1;
@@ -6672,11 +6612,33 @@ $('sequence-auto-end').onclick=function(){
  }else{
   setStatus('Auto Fim desligado');
  }
-};
-$('sequence-clear').onclick=requestDeleteLastSequenceItem;
-const sequenceClearAllButton=$('sequence-clear-all');
-if(sequenceClearAllButton)sequenceClearAllButton.onclick=clearActiveSequenceAll;
-$('sequence-toggle').onclick=()=>{sequencePanelVisible=!sequencePanelVisible;saveChordSequence();syncSequencePanelVisibility();setStatus(sequencePanelVisible?'Barra de sequências exibida':'Barra de sequências ocultada')};
+}
+function toggleSequencePanel(){sequencePanelVisible=!sequencePanelVisible;saveChordSequence();syncSequencePanelVisibility();setStatus(sequencePanelVisible?'Barra de sequências exibida':'Barra de sequências ocultada')}
+const sequencerUI=GeraSequencerUI.createController({
+ document:document,
+ getElement:$,
+ selectSection:selectSequenceSection,
+ adjustRepeat:adjustSequenceRepeat,
+ addPause:addSelectedSequencePause,
+ togglePlayback:toggleSelectedSequencePlayback,
+ toggleHoldLoop:toggleSequenceHoldLoop,
+ saveSectionControls:saveActiveSectionDrumControls,
+ changeSectionPattern:changeActiveSectionDrumPattern,
+ toggleDrums:toggleSequenceDrums,
+ toggleAuto:toggleSequenceAuto,
+ toggleAutoEnd:toggleSequenceAutoEnd,
+ deleteLast:requestDeleteLastSequenceItem,
+ clearAll:clearActiveSequenceAll,
+ togglePanel:toggleSequencePanel,
+ syncEditorKind:syncSequenceEditorKind,
+ commitEditor:commitSequenceEditor,
+ closeEditor:closeSequenceEditor,
+ confirmDelete:function(){return appConfirm('Excluir este item da sequência?','Excluir item')},
+ openOrganizer:openSequenceOrganizer,
+ resetOrder:resetSequenceOrder,
+ organizerDone:function(){setStatus('Ordem das sequências salva')}
+});
+sequencerUI.bindMainControls();
 $('advanced-toggle').onclick=()=>{$('advanced').classList.toggle('open');$('advanced-toggle').classList.toggle('active')};$('fullscreen').onclick=()=>{if(!document.fullscreenElement)document.documentElement.requestFullscreen?.();else document.exitFullscreen?.()};$('stop-accompaniments').onclick=requestAccompanimentStop;
 chordsCircleUI.bindLegacyControls();
 const FACTORY_SETTINGS={instrument:'piano',octave:0,sustainMode:'pressed',releaseMs:650,attackMs:20,chordMode:'together',arpInterval:280,arpPattern:'up',inversion:0,bassEnabled:false,latchEnabled:false,splitEnabled:false,splitInstrument:'organ',velocityEnabled:true,glissEnabled:true,bpm:100,rhythmPattern:'whole',chordChangeMode:'nextBar',drumEngine:'acoustic',values:{master:'68',keyboard:'92',chord:'72',bass:'56'}};
@@ -6899,28 +6861,7 @@ const appConfirmDialog=$('app-confirm-dialog');
 if(appConfirmOk)appConfirmOk.onclick=function(){closeAppConfirm(true)};
 if(appConfirmCancel)appConfirmCancel.onclick=function(){closeAppConfirm(false)};
 if(appConfirmDialog)appConfirmDialog.addEventListener('cancel',function(event){event.preventDefault();closeAppConfirm(false)});
-const sequenceEditKind=$('sequence-edit-kind');
-const sequenceEditBefore=$('sequence-edit-before');
-const sequenceEditReplace=$('sequence-edit-replace');
-const sequenceEditAfter=$('sequence-edit-after');
-const sequenceEditDelete=$('sequence-edit-delete');
-const sequenceEditCancel=$('sequence-edit-cancel');
-const sequenceEditClose=$('sequence-edit-close');
-const sequenceEditorDialog=$('sequence-editor-dialog');
-
-if(sequenceEditKind)sequenceEditKind.onchange=syncSequenceEditorKind;
-if(sequenceEditBefore)sequenceEditBefore.onclick=function(){commitSequenceEditor('before');closeSequenceEditor()};
-if(sequenceEditReplace)sequenceEditReplace.onclick=function(){commitSequenceEditor('replace');closeSequenceEditor()};
-if(sequenceEditAfter)sequenceEditAfter.onclick=function(){commitSequenceEditor('after');closeSequenceEditor()};
-if(sequenceEditDelete)sequenceEditDelete.onclick=async function(){
- if(await appConfirm('Excluir este item da sequência?','Excluir item'))commitSequenceEditor('delete');
-};
-if(sequenceEditCancel)sequenceEditCancel.onclick=closeSequenceEditor;
-if(sequenceEditClose)sequenceEditClose.onclick=closeSequenceEditor;
-if(sequenceEditorDialog)sequenceEditorDialog.addEventListener('cancel',function(event){
- event.preventDefault();
- closeSequenceEditor();
-});
+sequencerUI.bindItemEditor();
 const songListCreateButton=$('song-list-create');
 const songListEditorClose=$('song-list-editor-close');
 const songListEditorCancel=$('song-list-editor-cancel');
@@ -6940,16 +6881,7 @@ if(songListEditorDialog)songListEditorDialog.addEventListener('cancel',function(
  closeSongListEditor();
 });
 
-const sequenceOrganizeOpen=$('sequence-organize-open');
-const sequenceOrganizeClose=$('sequence-organize-close');
-const sequenceOrganizeDone=$('sequence-organize-done');
-const sequenceOrderReset=$('sequence-order-reset');
-const sequenceOrganizeDialog=$('sequence-organize-dialog');
-if(sequenceOrganizeOpen)sequenceOrganizeOpen.onclick=openSequenceOrganizer;
-if(sequenceOrganizeClose)sequenceOrganizeClose.onclick=function(){sequenceOrganizeDialog.close()};
-if(sequenceOrganizeDone)sequenceOrganizeDone.onclick=function(){sequenceOrganizeDialog.close();setStatus('Ordem das sequências salva')};
-if(sequenceOrderReset)sequenceOrderReset.onclick=resetSequenceOrder;
-if(sequenceOrganizeDialog)sequenceOrganizeDialog.addEventListener('cancel',function(event){event.preventDefault();sequenceOrganizeDialog.close()});
+sequencerUI.bindOrganizer();
 $('songs-open').onclick=()=>{renderSongsList();$('song-name-input').value=currentSongName||'';$('song-bpm-input').value=bpm;$('songs-dialog').showModal()};
 $('songs-close').onclick=()=>$('songs-dialog').close();
 $('song-save').onclick=async ()=>{
@@ -7767,66 +7699,43 @@ $('song-new').onclick=async ()=>{
    :'Gravação encerrada em '+SEQUENCE_SECTION_LABELS[activeSequenceSection]);
  }
  function bindSequenceRecordDialog(){
-  const dialog=byId('sequence-record-dialog');
-  if(!dialog)return;
-  const recordButton=byId('sequence-record');
-  if(recordButton){
-   recordButton.onclick=null;
-   recordButton.addEventListener('click',function(event){
-    event.preventDefault();
-    event.stopPropagation();
-    openSequenceRecordDialog();
-   });
-  }
-  byId('sequence-record-close').onclick=cancelSequenceRecordDialog;
-  byId('sequence-record-cancel').onclick=cancelSequenceRecordDialog;
-  byId('sequence-record-done').onclick=function(){
-   sequenceRecordCancelRequested=false;
-   closeSequenceRecordDialog();
-  };
-  byId('sequence-record-play').onclick=toggleSequenceRecordPreview;
-  byId('sequence-record-section-select').onchange=function(){selectRecordSection(this.value)};
-  document.querySelectorAll('[data-record-fraction]').forEach(function(button){button.onclick=function(){selectSequenceRecordFraction(Number(button.dataset.recordFraction))}});
-  byId('sequence-record-pause').onclick=function(){addPauseToSequence(sequenceRecordFraction);sequenceRecordLatestIndex=currentChordSequence().length-1;renderSequenceRecordExisting();syncSequenceRecordSummary()};
-  byId('sequence-record-undo').onclick=function(){
-   const items=currentChordSequence();
-   if(!items.length){setStatus('A sequência já está vazia');return}
-   const removed=items.pop();
-   saveChordSequence();
-   renderChordSequence();
-   sequenceRecordLatestIndex=items.length-1;
-   renderSequenceRecordExisting();
-   syncSequenceRecordSummary();
-   setStatus((removed&&removed.label?removed.label:'Último item')+' removido');
-  };
-
-  byId('sequence-record-clear-all').onclick=async function(){
-   const cleared=await clearActiveSequenceAll();
-   if(!cleared)return;
-   sequenceRecordStartLength=0;
-   sequenceRecordLatestIndex=-1;
-   renderSequenceRecordExisting();
-   syncSequenceRecordSummary();
-  };
-  dialog.addEventListener('close',finishSequenceRecordDialog);
-  dialog.addEventListener('cancel',function(event){
-   event.preventDefault();
-   cancelSequenceRecordDialog();
-  });
-  dialog.addEventListener('pointerup',finishSequenceRecordPointer);
-  dialog.addEventListener('pointercancel',finishSequenceRecordPointer);
-
-  const editorDialog=byId('sequence-editor-dialog');
-  if(editorDialog&&editorDialog.dataset.recordRefreshBound!=='true'){
-   editorDialog.dataset.recordRefreshBound='true';
-   editorDialog.addEventListener('close',function(){
+  const recordUI=GeraSequencerUI.createController({
+   document:document,
+   getElement:byId,
+   openRecordDialog:openSequenceRecordDialog,
+   cancelRecordDialog:cancelSequenceRecordDialog,
+   finishRecordDialog:function(){sequenceRecordCancelRequested=false;closeSequenceRecordDialog()},
+   toggleRecordPreview:toggleSequenceRecordPreview,
+   selectRecordSection:selectRecordSection,
+   selectRecordFraction:selectSequenceRecordFraction,
+   addRecordPause:function(){addPauseToSequence(sequenceRecordFraction);sequenceRecordLatestIndex=currentChordSequence().length-1;renderSequenceRecordExisting();syncSequenceRecordSummary()},
+   undoRecordItem:function(){
+    const items=currentChordSequence();
+    if(!items.length){setStatus('A sequência já está vazia');return}
+    const removed=items.pop();
+    saveChordSequence();
+    renderChordSequence();
+    sequenceRecordLatestIndex=items.length-1;
+    renderSequenceRecordExisting();
+    syncSequenceRecordSummary();
+    setStatus((removed&&removed.label?removed.label:'Último item')+' removido');
+   },
+   clearRecordItems:async function(){
+    const cleared=await clearActiveSequenceAll();
+    if(!cleared)return;
+    sequenceRecordStartLength=0;
+    sequenceRecordLatestIndex=-1;
+    renderSequenceRecordExisting();
+    syncSequenceRecordSummary();
+   },
+   recordDialogClosed:finishSequenceRecordDialog,
+   finishRecordPointer:finishSequenceRecordPointer,
+   refreshRecordAfterEditor:function(){
     const recordDialog=byId('sequence-record-dialog');
-    if(recordDialog&&recordDialog.open){
-     renderSequenceRecordExisting();
-     syncSequenceRecordSummary();
-    }
-   });
-  }
+    if(recordDialog&&recordDialog.open){renderSequenceRecordExisting();syncSequenceRecordSummary()}
+   }
+  });
+  recordUI.bindRecordDialog();
  }
 
  function assemble(){
diff --git a/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.25-etapa-8E-bateria/manifest.json b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/manifest.json
index e3a43fd..8f21e6d 100644
--- a/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.25-etapa-8E-bateria/manifest.json
+++ b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/manifest.json
@@ -39,5 +39,5 @@
             "purpose": "maskable"
         }
     ],
-    "version": "3.15.25"
+    "version": "3.15.26"
 }
diff --git a/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.25-etapa-8E-bateria/sw.js b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/sw.js
index c4b0090..2429d66 100644
--- a/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.25-etapa-8E-bateria/sw.js
+++ b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/sw.js
@@ -2,7 +2,7 @@
 
 const CACHE_PREFIX = 'gera-pwa-';
 const LEGACY_CACHE_PREFIX = 'teclado-virtual-pwa-';
-const CACHE_NAME = CACHE_PREFIX + 'v3.15.25';
+const CACHE_NAME = CACHE_PREFIX + 'v3.15.26';
 const OFFLINE_URL = './offline.html';
 
 const PRECACHE_URLS = [
@@ -28,6 +28,7 @@ const PRECACHE_URLS = [
     "./js/ui/keyboard.js",
     "./js/ui/chords-circle.js",
     "./js/ui/drums.js",
+    "./js/ui/sequencer.js",
     "./js/audio/core.js",
     "./kit-acustico-selecionado/MAPEAMENTO.txt",
     "./kit-acustico-selecionado/cymbals/crash-1.wav",
diff --git a/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/js/ui/sequencer.js b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/js/ui/sequencer.js
new file mode 100644
index 0000000..17cd8e7
--- /dev/null
+++ b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/js/ui/sequencer.js
@@ -0,0 +1,204 @@
+/*
+ * GERA — interface do sequenciador.
+ * Etapa 8F: concentra ligações e estados visuais sem decidir áudio ou transporte.
+ */
+(function(global){
+ 'use strict';
+
+ function createController(options){
+  const documentRef=options.document||global.document;
+  const element=options.getElement;
+
+  function syncRepeatInputs(repeatValue){
+   documentRef.querySelectorAll('[data-section-repeat-display]').forEach(function(display){
+    const section=display.dataset.sectionRepeatDisplay;
+    const value=repeatValue(section);
+    display.textContent=String(value);
+    display.title=value===0?'Fora do modo Auto':value+' repetições';
+   });
+  }
+  function syncHoldLoop(active){
+   const button=element('sequence-hold-loop');
+   if(!button)return;
+   button.classList.toggle('active',active);
+   button.textContent=active?'🔁 Em Loop':'🔁 Pôr em Loop';
+   button.title=active?'Clique para liberar a progressão':'Manter a seção atual em repetição';
+  }
+  function syncAuto(auto,autoEnd){
+   const loopButton=element('sequence-auto');
+   const endButton=element('sequence-auto-end');
+   if(loopButton){
+    loopButton.classList.toggle('active',auto);
+    loopButton.textContent=auto?'Auto ✓':'Auto';
+    loopButton.title=auto
+     ?'Auto contínuo ligado: ao final volta para a primeira sequência'
+     :'Ligar execução automática contínua';
+   }
+   if(endButton){
+    endButton.classList.toggle('active',autoEnd);
+    endButton.textContent=autoEnd?'Auto Fim ✓':'Auto Fim';
+    endButton.title=autoEnd
+     ?'Auto Fim ligado: executa uma vez e encerra na última sequência'
+     :'Executar a música uma vez e encerrar com virada de bateria';
+   }
+  }
+  function syncPanelVisibility(visible){
+   const panel=element('sequence-panel');
+   const button=element('sequence-toggle');
+   if(!panel||!button)return;
+   panel.classList.toggle('sequence-hidden',!visible);
+   button.classList.toggle('sequence-toggle-active',visible);
+   button.title=visible?'Ocultar barra de sequências':'Mostrar barra de sequências';
+  }
+  function syncRecordButton(recording){
+   const button=element('sequence-record');
+   if(!button)return;
+   button.classList.toggle('sequence-recording',recording);
+   button.textContent=recording?'■ Gravando':'● Gravar';
+  }
+  function syncPauseButton(state){
+   const button=element('sequence-rest');
+   if(!button)return;
+   button.textContent='⏸ Pausa '+state.fractionLabel;
+   button.disabled=state.fraction===0;
+   button.title=state.fraction===0
+    ?'Selecione um tempo de ⅛ a 1 para adicionar uma pausa'
+    :'Adicionar pausa de '+state.durationLabel;
+  }
+  function syncPlayButton(playing){
+   const button=element('sequence-play');
+   if(!button)return;
+   button.classList.toggle('sequence-play-active',playing);
+   button.classList.remove('sequence-stop-pending');
+   button.textContent=playing?'⏹ Parar Sequência':'▶ Tocar Sequência';
+   button.title=playing?'Parar imediatamente a sequência em execução':'Reproduzir a sequência selecionada';
+   button.setAttribute('aria-pressed',String(playing));
+  }
+  function syncDrumButton(active){
+   const button=element('sequence-stop-drums');
+   if(!button)return;
+   button.classList.toggle('sequence-drums-active',active);
+   button.textContent='🥁';
+   button.setAttribute('aria-label',active?'Parar bateria':'Iniciar bateria');
+   button.setAttribute('aria-pressed',String(active));
+   button.title=active?'Parar somente a bateria':'Iniciar a bateria configurada para a sequência selecionada';
+  }
+  function syncSectionButtons(state){
+   documentRef.querySelectorAll('[data-sequence-section]').forEach(function(button){
+    const key=button.dataset.sequenceSection;
+    button.classList.toggle('active-section',key===state.activeSection);
+    button.classList.toggle('queued-section',state.playing&&key===state.queuedSection);
+    button.classList.toggle('has-content',state.hasContent(key));
+    button.setAttribute('aria-label',state.labels[key]+(state.playing&&key===state.queuedSection?' — próxima seção':''));
+   });
+   documentRef.querySelectorAll('[data-section-control]').forEach(function(control){
+    control.classList.toggle('playing-repeat',state.playing&&control.dataset.sectionControl===state.activeSection);
+   });
+  }
+  function bindMainControls(){
+   documentRef.querySelectorAll('[data-sequence-section]').forEach(function(button){
+    button.onclick=function(){options.selectSection(button.dataset.sequenceSection)};
+   });
+   documentRef.querySelectorAll('[data-repeat-adjust]').forEach(function(button){
+    button.onclick=function(){options.adjustRepeat(button.dataset.repeatAdjust,Number(button.dataset.delta))};
+   });
+   element('sequence-rest').onclick=function(){options.addPause()};
+   element('sequence-play').onclick=function(){options.togglePlayback()};
+   element('sequence-hold-loop').onclick=function(){options.toggleHoldLoop()};
+   element('section-instrument').onchange=function(){options.saveSectionControls()};
+   element('section-next').onchange=function(){options.saveSectionControls()};
+   element('section-drum-pattern').onchange=function(){options.changeSectionPattern(this.value)};
+   element('section-drum-entry').onchange=function(){options.saveSectionControls()};
+   element('section-drum-exit').onchange=function(){options.saveSectionControls()};
+   element('section-drum-final').onchange=function(){options.saveSectionControls()};
+   element('sequence-stop-drums').onclick=function(){options.toggleDrums()};
+   element('sequence-auto').onclick=function(){options.toggleAuto()};
+   element('sequence-auto-end').onclick=function(){options.toggleAutoEnd()};
+   element('sequence-clear').onclick=function(){options.deleteLast()};
+   const clearAll=element('sequence-clear-all');
+   if(clearAll)clearAll.onclick=function(){options.clearAll()};
+   element('sequence-toggle').onclick=function(){options.togglePanel()};
+  }
+  function bindItemEditor(){
+   const kind=element('sequence-edit-kind');
+   const before=element('sequence-edit-before');
+   const replace=element('sequence-edit-replace');
+   const after=element('sequence-edit-after');
+   const remove=element('sequence-edit-delete');
+   const cancel=element('sequence-edit-cancel');
+   const close=element('sequence-edit-close');
+   const dialog=element('sequence-editor-dialog');
+   if(kind)kind.onchange=function(){options.syncEditorKind()};
+   if(before)before.onclick=function(){options.commitEditor('before');options.closeEditor()};
+   if(replace)replace.onclick=function(){options.commitEditor('replace');options.closeEditor()};
+   if(after)after.onclick=function(){options.commitEditor('after');options.closeEditor()};
+   if(remove)remove.onclick=async function(){if(await options.confirmDelete())options.commitEditor('delete')};
+   if(cancel)cancel.onclick=function(){options.closeEditor()};
+   if(close)close.onclick=function(){options.closeEditor()};
+   if(dialog)dialog.addEventListener('cancel',function(event){event.preventDefault();options.closeEditor()});
+  }
+  function bindOrganizer(){
+   const open=element('sequence-organize-open');
+   const close=element('sequence-organize-close');
+   const done=element('sequence-organize-done');
+   const reset=element('sequence-order-reset');
+   const dialog=element('sequence-organize-dialog');
+   if(open)open.onclick=function(){options.openOrganizer()};
+   if(close)close.onclick=function(){dialog.close()};
+   if(done)done.onclick=function(){dialog.close();options.organizerDone()};
+   if(reset)reset.onclick=function(){options.resetOrder()};
+   if(dialog)dialog.addEventListener('cancel',function(event){event.preventDefault();dialog.close()});
+  }
+  function bindRecordDialog(){
+   const dialog=element('sequence-record-dialog');
+   if(!dialog)return;
+   const recordButton=element('sequence-record');
+   if(recordButton){
+    recordButton.onclick=null;
+    recordButton.addEventListener('click',function(event){
+     event.preventDefault();
+     event.stopPropagation();
+     options.openRecordDialog();
+    });
+   }
+   element('sequence-record-close').onclick=function(){options.cancelRecordDialog()};
+   element('sequence-record-cancel').onclick=function(){options.cancelRecordDialog()};
+   element('sequence-record-done').onclick=function(){options.finishRecordDialog()};
+   element('sequence-record-play').onclick=function(){options.toggleRecordPreview()};
+   element('sequence-record-section-select').onchange=function(){options.selectRecordSection(this.value)};
+   documentRef.querySelectorAll('[data-record-fraction]').forEach(function(button){
+    button.onclick=function(){options.selectRecordFraction(Number(button.dataset.recordFraction))};
+   });
+   element('sequence-record-pause').onclick=function(){options.addRecordPause()};
+   element('sequence-record-undo').onclick=function(){options.undoRecordItem()};
+   element('sequence-record-clear-all').onclick=function(){options.clearRecordItems()};
+   dialog.addEventListener('close',function(){options.recordDialogClosed()});
+   dialog.addEventListener('cancel',function(event){event.preventDefault();options.cancelRecordDialog()});
+   dialog.addEventListener('pointerup',function(event){options.finishRecordPointer(event)});
+   dialog.addEventListener('pointercancel',function(event){options.finishRecordPointer(event)});
+   const editorDialog=element('sequence-editor-dialog');
+   if(editorDialog&&editorDialog.dataset.recordRefreshBound!=='true'){
+    editorDialog.dataset.recordRefreshBound='true';
+    editorDialog.addEventListener('close',function(){options.refreshRecordAfterEditor()});
+   }
+  }
+
+  return Object.freeze({
+   bindMainControls:bindMainControls,
+   bindItemEditor:bindItemEditor,
+   bindOrganizer:bindOrganizer,
+   bindRecordDialog:bindRecordDialog,
+   syncRepeatInputs:syncRepeatInputs,
+   syncHoldLoop:syncHoldLoop,
+   syncAuto:syncAuto,
+   syncPanelVisibility:syncPanelVisibility,
+   syncRecordButton:syncRecordButton,
+   syncPauseButton:syncPauseButton,
+   syncPlayButton:syncPlayButton,
+   syncDrumButton:syncDrumButton,
+   syncSectionButtons:syncSectionButtons
+  });
+ }
+
+ global.GeraSequencerUI=Object.freeze({createController:createController});
+})(typeof window!=='undefined'?window:globalThis);
diff --git a/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.25-etapa-8E-bateria/tests/audit-parity.test.js b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/tests/audit-parity.test.js
index 0ba258b..eaca39c 100644
--- a/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.25-etapa-8E-bateria/tests/audit-parity.test.js
+++ b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/tests/audit-parity.test.js
@@ -144,9 +144,9 @@ test('auditoria 6K preservada e arquivos fora do armazenamento seguem idênticos
  assert.equal(restoredManifest,fs.readFileSync(path.join(previous,'manifest.json'),'utf8'));
 });
 
-test('cache 3.15.25 preserva exatamente os módulos carregados pelo navegador',function(){
+test('cache 3.15.26 preserva exatamente os módulos carregados pelo navegador',function(){
  const sw=fs.readFileSync(path.join(root,'sw.js'),'utf8');
- assert.ok(sw.includes("const CACHE_NAME = CACHE_PREFIX + 'v3.15.25';"));
+ assert.ok(sw.includes("const CACHE_NAME = CACHE_PREFIX + 'v3.15.26';"));
  ['clock','scheduler','boundaries','tempo','drum-sync','chord-sequence-sync','sequence-transitions','coordinator'].forEach(function(name){assert.ok(sw.includes('"./js/transport/'+name+'.js"'))});
  assert.ok(sw.includes('"./js/ui/transport-status.js"'));
  assert.ok(sw.includes('"./js/ui/header.js"'));
diff --git a/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.25-etapa-8E-bateria/tests/boundaries.test.js b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/tests/boundaries.test.js
index fc68a95..d6dd7f5 100644
--- a/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.25-etapa-8E-bateria/tests/boundaries.test.js
+++ b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/tests/boundaries.test.js
@@ -119,7 +119,7 @@ test('preserva a integração das fronteiras no SERVICE WORKER e no manifesto',f
  const currentSw=fs.readFileSync(path.join(root,'sw.js'),'utf8');
  assert.ok(currentSw.includes('    "./js/transport/boundaries.js",'));
  const currentManifest=JSON.parse(fs.readFileSync(path.join(root,'manifest.json'),'utf8'));
- assert.equal(currentManifest.version,'3.15.25');
+ assert.equal(currentManifest.version,'3.15.26');
 });
 
 test('todos os demais recursos funcionais permanecem byte a byte iguais',function(){
diff --git a/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.25-etapa-8E-bateria/tests/chord-sequence-sync.test.js b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/tests/chord-sequence-sync.test.js
index 6c14d07..de60a61 100644
--- a/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.25-etapa-8E-bateria/tests/chord-sequence-sync.test.js
+++ b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/tests/chord-sequence-sync.test.js
@@ -171,10 +171,10 @@ test('recursos funcionais fora dos pontos autorizados permanecem byte a byte igu
  files.forEach(function(file){assert.deepEqual(fs.readFileSync(path.join(root,file)),fs.readFileSync(path.join(baselineRoot,file)),file)});
 });
 
-test('SERVICE WORKER mantém o módulo da sequência no cache 3.15.25',function(){
+test('SERVICE WORKER mantém o módulo da sequência no cache 3.15.26',function(){
  const currentSw=fs.readFileSync(path.join(root,'sw.js'),'utf8');
  const baselineSw=fs.readFileSync(path.join(baselineRoot,'sw.js'),'utf8');
- const reconstructed=currentSw.replace("'v3.15.25'","'v3.15.09'")
+ const reconstructed=currentSw.replace("'v3.15.26'","'v3.15.09'")
   .replace('    "./js/storage.js",\n','')
   .replace('    "./js/transport/chord-sequence-sync.js",\n','')
   .replace('    "./js/transport/sequence-transitions.js",\n','')
@@ -185,9 +185,10 @@ test('SERVICE WORKER mantém o módulo da sequência no cache 3.15.25',function(
   .replace('    "./js/ui/compact-panel.js",\n','')
   .replace('    "./js/ui/keyboard.js",\n','')
   .replace('    "./js/ui/chords-circle.js",\n','')
-  .replace('    "./js/ui/drums.js",\n','');
+  .replace('    "./js/ui/drums.js",\n','')
+  .replace('    "./js/ui/sequencer.js",\n','');
  assert.equal(restored,baselineSw);
- assert.equal(JSON.parse(fs.readFileSync(path.join(root,'manifest.json'),'utf8')).version,'3.15.25');
+ assert.equal(JSON.parse(fs.readFileSync(path.join(root,'manifest.json'),'utf8')).version,'3.15.26');
 });
 
 test('núcleo mantém reprodução interna e transições fora do módulo extraído',function(){
diff --git a/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.25-etapa-8E-bateria/tests/chords-circle.test.js b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/tests/chords-circle.test.js
index e6ff6ef..44b3a5b 100644
--- a/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.25-etapa-8E-bateria/tests/chords-circle.test.js
+++ b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/tests/chords-circle.test.js
@@ -167,11 +167,11 @@ test('HTML carrega o módulo antes do núcleo e remove as rotinas visuais direta
  assert.equal(html.includes('function selectRedesignCircle(root){'),false);
 });
 
-test('SERVICE WORKER inclui o módulo uma vez e usa a versão 3.15.25',function(){
+test('SERVICE WORKER inclui o módulo uma vez e usa a versão 3.15.26',function(){
  const sw=fs.readFileSync(path.join(root,'sw.js'),'utf8');
- assert.ok(sw.includes("const CACHE_NAME = CACHE_PREFIX + 'v3.15.25';"));
+ assert.ok(sw.includes("const CACHE_NAME = CACHE_PREFIX + 'v3.15.26';"));
  assert.equal((sw.match(/\.\/js\/ui\/chords-circle\.js/g)||[]).length,1);
- assert.equal(JSON.parse(fs.readFileSync(path.join(root,'manifest.json'),'utf8')).version,'3.15.25');
+ assert.equal(JSON.parse(fs.readFileSync(path.join(root,'manifest.json'),'utf8')).version,'3.15.26');
 });
 
 test('reversão exclusiva da 8D recompõe a versão 3.15.23 byte a byte',function(){
diff --git a/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.25-etapa-8E-bateria/tests/clock.test.js b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/tests/clock.test.js
index 03422f4..44f2141 100644
--- a/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.25-etapa-8E-bateria/tests/clock.test.js
+++ b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/tests/clock.test.js
@@ -119,7 +119,7 @@ test('o evento activate remove caches antigos e preserva o cache atual e caches
   Promise:Promise,
   fetch:function(){return Promise.reject(new Error('não usado'))},
   caches:{
-   keys:function(){return Promise.resolve(['gera-pwa-v3.15.04','gera-pwa-v3.15.05','gera-pwa-v3.15.06','gera-pwa-v3.15.07','gera-pwa-v3.15.08','gera-pwa-v3.15.09','gera-pwa-v3.15.10','gera-pwa-v3.15.12','gera-pwa-v3.15.13','gera-pwa-v3.15.16','gera-pwa-v3.15.20','gera-pwa-v3.15.21','gera-pwa-v3.15.22','gera-pwa-v3.15.23','gera-pwa-v3.15.24','gera-pwa-v3.15.25','teclado-virtual-pwa-v3.14.97','outro-cache'])},
+   keys:function(){return Promise.resolve(['gera-pwa-v3.15.04','gera-pwa-v3.15.05','gera-pwa-v3.15.06','gera-pwa-v3.15.07','gera-pwa-v3.15.08','gera-pwa-v3.15.09','gera-pwa-v3.15.10','gera-pwa-v3.15.12','gera-pwa-v3.15.13','gera-pwa-v3.15.16','gera-pwa-v3.15.20','gera-pwa-v3.15.21','gera-pwa-v3.15.22','gera-pwa-v3.15.23','gera-pwa-v3.15.24','gera-pwa-v3.15.25','gera-pwa-v3.15.26','teclado-virtual-pwa-v3.14.97','outro-cache'])},
    delete:function(name){deleted.push(name);return Promise.resolve(true)},
    open:function(){return Promise.resolve({})},
    match:function(){return Promise.resolve(undefined)}
@@ -136,5 +136,5 @@ test('o evento activate remove caches antigos e preserva o cache atual e caches
  let activation;
  listeners.activate({waitUntil:function(promise){activation=promise}});
  await activation;
- assert.deepEqual(deleted,['gera-pwa-v3.15.04','gera-pwa-v3.15.05','gera-pwa-v3.15.06','gera-pwa-v3.15.07','gera-pwa-v3.15.08','gera-pwa-v3.15.09','gera-pwa-v3.15.10','gera-pwa-v3.15.12','gera-pwa-v3.15.13','gera-pwa-v3.15.16','gera-pwa-v3.15.20','gera-pwa-v3.15.21','gera-pwa-v3.15.22','gera-pwa-v3.15.23','gera-pwa-v3.15.24','teclado-virtual-pwa-v3.14.97']);
+ assert.deepEqual(deleted,['gera-pwa-v3.15.04','gera-pwa-v3.15.05','gera-pwa-v3.15.06','gera-pwa-v3.15.07','gera-pwa-v3.15.08','gera-pwa-v3.15.09','gera-pwa-v3.15.10','gera-pwa-v3.15.12','gera-pwa-v3.15.13','gera-pwa-v3.15.16','gera-pwa-v3.15.20','gera-pwa-v3.15.21','gera-pwa-v3.15.22','gera-pwa-v3.15.23','gera-pwa-v3.15.24','gera-pwa-v3.15.25','teclado-virtual-pwa-v3.14.97']);
 });
diff --git a/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.25-etapa-8E-bateria/tests/compact-panel.test.js b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/tests/compact-panel.test.js
index d4a3711..2c297df 100644
--- a/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.25-etapa-8E-bateria/tests/compact-panel.test.js
+++ b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/tests/compact-panel.test.js
@@ -91,11 +91,11 @@ test('HTML carrega o painel antes do núcleo e remove a ligação direta anterio
  assert.equal(html.includes('if(compactCarouselNext)compactCarouselNext.onclick='),false);
 });
 
-test('SERVICE WORKER mantém o painel compacto no pré-cache 3.15.25',function(){
+test('SERVICE WORKER mantém o painel compacto no pré-cache 3.15.26',function(){
  const sw=fs.readFileSync(path.join(root,'sw.js'),'utf8');
- assert.ok(sw.includes("const CACHE_NAME = CACHE_PREFIX + 'v3.15.25';"));
+ assert.ok(sw.includes("const CACHE_NAME = CACHE_PREFIX + 'v3.15.26';"));
  assert.equal((sw.match(/\.\/js\/ui\/compact-panel\.js/g)||[]).length,1);
- assert.equal(JSON.parse(fs.readFileSync(path.join(root,'manifest.json'),'utf8')).version,'3.15.25');
+ assert.equal(JSON.parse(fs.readFileSync(path.join(root,'manifest.json'),'utf8')).version,'3.15.26');
 });
 
 test('reversão exclusiva da 8B recompõe a versão 3.15.21 byte a byte',function(){
diff --git a/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.25-etapa-8E-bateria/tests/coordinator.test.js b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/tests/coordinator.test.js
index 3ce69bf..d92c551 100644
--- a/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.25-etapa-8E-bateria/tests/coordinator.test.js
+++ b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/tests/coordinator.test.js
@@ -140,9 +140,9 @@ test('coordenador permanece byte a byte igual à versão validada 3.15.11',funct
  assert.ok(index.includes('const transportCoordinator=GeraTransportCoordinator.createCoordinator({'));
 });
 
-test('SERVICE WORKER preserva o coordenador e usa o cache 3.15.25',function(){
+test('SERVICE WORKER preserva o coordenador e usa o cache 3.15.26',function(){
  const sw=fs.readFileSync(path.join(root,'sw.js'),'utf8');
  assert.ok(sw.includes('    "./js/transport/coordinator.js",'));
- assert.ok(sw.includes("const CACHE_NAME = CACHE_PREFIX + 'v3.15.25';"));
- assert.equal(JSON.parse(fs.readFileSync(path.join(root,'manifest.json'),'utf8')).version,'3.15.25');
+ assert.ok(sw.includes("const CACHE_NAME = CACHE_PREFIX + 'v3.15.26';"));
+ assert.equal(JSON.parse(fs.readFileSync(path.join(root,'manifest.json'),'utf8')).version,'3.15.26');
 });
diff --git a/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.25-etapa-8E-bateria/tests/drum-sync.test.js b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/tests/drum-sync.test.js
index c55ce0d..f57ecaa 100644
--- a/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.25-etapa-8E-bateria/tests/drum-sync.test.js
+++ b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/tests/drum-sync.test.js
@@ -163,7 +163,7 @@ test('módulo da bateria permanece no SERVICE WORKER e no núcleo após a etapa
  assert.ok(currentSw.includes('    "./js/transport/drum-sync.js",'));
  assert.ok(index.includes('const drumTransportConsumer=GeraTransportDrumSync.createConsumer({'));
  const manifest=JSON.parse(fs.readFileSync(path.join(root,'manifest.json'),'utf8'));
- assert.equal(manifest.version,'3.15.25');
+ assert.equal(manifest.version,'3.15.26');
 });
 
 test('módulo da bateria permanece byte a byte igual à versão validada 3.15.09',function(){
diff --git a/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.25-etapa-8E-bateria/tests/drums-ui.test.js b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/tests/drums-ui.test.js
index 5eeccc8..9650c57 100644
--- a/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.25-etapa-8E-bateria/tests/drums-ui.test.js
+++ b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/tests/drums-ui.test.js
@@ -151,16 +151,17 @@ test('HTML carrega o módulo e delega a interface da bateria',function(){
  assert.equal(html.includes("document.querySelectorAll('[data-layer]').forEach"),false);
 });
 
-test('SERVICE WORKER inclui o módulo uma vez e usa a versão 3.15.25',function(){
+test('SERVICE WORKER inclui o módulo uma vez e usa a versão 3.15.26',function(){
  const sw=fs.readFileSync(path.join(root,'sw.js'),'utf8');
- assert.ok(sw.includes("const CACHE_NAME = CACHE_PREFIX + 'v3.15.25';"));
+ assert.ok(sw.includes("const CACHE_NAME = CACHE_PREFIX + 'v3.15.26';"));
  assert.equal((sw.match(/\.\/js\/ui\/drums\.js/g)||[]).length,1);
- assert.equal(JSON.parse(fs.readFileSync(path.join(root,'manifest.json'),'utf8')).version,'3.15.25');
+ assert.equal(JSON.parse(fs.readFileSync(path.join(root,'manifest.json'),'utf8')).version,'3.15.26');
 });
 
 test('módulo da 8E é a única adição de arquivo funcional sobre a 3.15.24',function(){
  const previous=path.resolve(root,'..','GERA-PWA-v3.15.24-etapa-8D-acordes-circulo-harmonico');
- const currentFiles=fs.readdirSync(path.join(root,'js','ui')).sort();
+ const stage8E=path.resolve(root,'..','GERA-PWA-v3.15.25-etapa-8E-bateria');
+ const currentFiles=fs.readdirSync(path.join(stage8E,'js','ui')).sort();
  const previousFiles=fs.readdirSync(path.join(previous,'js','ui')).sort();
  assert.deepEqual(currentFiles.filter(function(file){return previousFiles.indexOf(file)<0}),['drums.js']);
  assert.equal(fs.existsSync(path.join(previous,'js','ui','drums.js')),false);
@@ -168,8 +169,9 @@ test('módulo da 8E é a única adição de arquivo funcional sobre a 3.15.24',f
 
 test('reversão exclusiva da 8E recompõe a versão 3.15.24 byte a byte',function(){
  const previous=path.resolve(root,'..','GERA-PWA-v3.15.24-etapa-8D-acordes-circulo-harmonico');
+ const stage8E=path.resolve(root,'..','GERA-PWA-v3.15.25-etapa-8E-bateria');
  const previousIndex=fs.readFileSync(path.join(previous,'index.html'),'utf8');
- let reconstructed=fs.readFileSync(path.join(root,'index.html'),'utf8');
+ let reconstructed=fs.readFileSync(path.join(stage8E,'index.html'),'utf8');
 
  function replaceRange(current,currentStart,currentEnd,original,originalStart,originalEnd){
   const a=current.indexOf(currentStart);
@@ -192,10 +194,10 @@ test('reversão exclusiva da 8E recompõe a versão 3.15.24 byte a byte',functio
   .replaceAll('3.15.25','3.15.24')
   .replace('<script src="./js/ui/drums.js"></script>\n','');
 
- const reconstructedSw=fs.readFileSync(path.join(root,'sw.js'),'utf8')
+ const reconstructedSw=fs.readFileSync(path.join(stage8E,'sw.js'),'utf8')
   .replace("'v3.15.25'","'v3.15.24'")
   .replace('    "./js/ui/drums.js",\n','');
- const reconstructedManifest=fs.readFileSync(path.join(root,'manifest.json'),'utf8')
+ const reconstructedManifest=fs.readFileSync(path.join(stage8E,'manifest.json'),'utf8')
   .replace('"3.15.25"','"3.15.24"');
 
  assert.equal(reconstructed,previousIndex);
diff --git a/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.25-etapa-8E-bateria/tests/header.test.js b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/tests/header.test.js
index db74e61..2dbb360 100644
--- a/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.25-etapa-8E-bateria/tests/header.test.js
+++ b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/tests/header.test.js
@@ -81,11 +81,11 @@ test('HTML carrega o cabeçalho antes do bloco de montagem e remove listeners du
  assert.equal(html.includes("byId('redesign-song-pill').onclick="),false);
 });
 
-test('SERVICE WORKER mantém o módulo do cabeçalho no pré-cache 3.15.25',function(){
+test('SERVICE WORKER mantém o módulo do cabeçalho no pré-cache 3.15.26',function(){
  const sw=fs.readFileSync(path.join(root,'sw.js'),'utf8');
- assert.ok(sw.includes("const CACHE_NAME = CACHE_PREFIX + 'v3.15.25';"));
+ assert.ok(sw.includes("const CACHE_NAME = CACHE_PREFIX + 'v3.15.26';"));
  assert.equal((sw.match(/\.\/js\/ui\/header\.js/g)||[]).length,1);
- assert.equal(JSON.parse(fs.readFileSync(path.join(root,'manifest.json'),'utf8')).version,'3.15.25');
+ assert.equal(JSON.parse(fs.readFileSync(path.join(root,'manifest.json'),'utf8')).version,'3.15.26');
 });
 
 test('reversão exclusiva da 8A recompõe a versão 3.15.20 byte a byte',function(){
diff --git a/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.25-etapa-8E-bateria/tests/keyboard.test.js b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/tests/keyboard.test.js
index ecd0578..2516833 100644
--- a/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.25-etapa-8E-bateria/tests/keyboard.test.js
+++ b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/tests/keyboard.test.js
@@ -122,11 +122,11 @@ test('HTML carrega o teclado antes do núcleo e remove os listeners diretos ante
  assert.equal(html.includes("keyboardEl.addEventListener('touchstart'"),false);
 });
 
-test('SERVICE WORKER mantém o teclado no pré-cache 3.15.25',function(){
+test('SERVICE WORKER mantém o teclado no pré-cache 3.15.26',function(){
  const sw=fs.readFileSync(path.join(root,'sw.js'),'utf8');
- assert.ok(sw.includes("const CACHE_NAME = CACHE_PREFIX + 'v3.15.25';"));
+ assert.ok(sw.includes("const CACHE_NAME = CACHE_PREFIX + 'v3.15.26';"));
  assert.equal((sw.match(/\.\/js\/ui\/keyboard\.js/g)||[]).length,1);
- assert.equal(JSON.parse(fs.readFileSync(path.join(root,'manifest.json'),'utf8')).version,'3.15.25');
+ assert.equal(JSON.parse(fs.readFileSync(path.join(root,'manifest.json'),'utf8')).version,'3.15.26');
 });
 
 test('reversão exclusiva da 8C recompõe a versão 3.15.22 byte a byte',function(){
diff --git a/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.25-etapa-8E-bateria/tests/sequence-transitions.test.js b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/tests/sequence-transitions.test.js
index 1ff5363..b8d451b 100644
--- a/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.25-etapa-8E-bateria/tests/sequence-transitions.test.js
+++ b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/tests/sequence-transitions.test.js
@@ -172,9 +172,9 @@ test('reversão exclusiva da 6I recompõe a versão 3.15.11 byte a byte',functio
  assert.equal(reconstructedManifest,fs.readFileSync(path.join(baselineRoot,'manifest.json'),'utf8'));
 });
 
-test('módulo de transições permanece no pré-cache da versão 3.15.25',function(){
+test('módulo de transições permanece no pré-cache da versão 3.15.26',function(){
  const sw=fs.readFileSync(path.join(root,'sw.js'),'utf8');
  assert.ok(sw.includes('    "./js/transport/sequence-transitions.js",'));
- assert.ok(sw.includes("const CACHE_NAME = CACHE_PREFIX + 'v3.15.25';"));
- assert.equal(JSON.parse(fs.readFileSync(path.join(root,'manifest.json'),'utf8')).version,'3.15.25');
+ assert.ok(sw.includes("const CACHE_NAME = CACHE_PREFIX + 'v3.15.26';"));
+ assert.equal(JSON.parse(fs.readFileSync(path.join(root,'manifest.json'),'utf8')).version,'3.15.26');
 });
diff --git a/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/tests/sequencer-ui.test.js b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/tests/sequencer-ui.test.js
new file mode 100644
index 0000000..7e50d24
--- /dev/null
+++ b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/tests/sequencer-ui.test.js
@@ -0,0 +1,205 @@
+'use strict';
+
+const test=require('node:test');
+const assert=require('node:assert/strict');
+const fs=require('node:fs');
+const path=require('node:path');
+const vm=require('node:vm');
+
+const root=path.resolve(__dirname,'..');
+const source=fs.readFileSync(path.join(root,'js/ui/sequencer.js'),'utf8');
+
+function fakeElement(id,dataset){
+ const classes=new Set();
+ const listeners={};
+ return{
+  id:id||'',dataset:dataset||{},textContent:'',title:'',value:'',disabled:false,open:true,
+  attributes:{},listeners:listeners,closeCalls:0,
+  classList:{
+   add:function(name){classes.add(name)},remove:function(name){classes.delete(name)},
+   toggle:function(name,force){const active=force===undefined?!classes.has(name):Boolean(force);if(active)classes.add(name);else classes.delete(name);return active},
+   contains:function(name){return classes.has(name)}
+  },
+  setAttribute:function(name,value){this.attributes[name]=String(value)},
+  addEventListener:function(type,handler){listeners[type]=handler},
+  close:function(){this.open=false;this.closeCalls++}
+ };
+}
+
+function harness(){
+ const ids=['sequence-hold-loop','sequence-auto','sequence-auto-end','sequence-panel','sequence-toggle','sequence-record','sequence-rest',
+  'sequence-play','sequence-stop-drums','section-instrument','section-next','section-drum-pattern','section-drum-entry','section-drum-exit',
+  'section-drum-final','sequence-clear','sequence-clear-all','sequence-edit-kind','sequence-edit-before','sequence-edit-replace',
+  'sequence-edit-after','sequence-edit-delete','sequence-edit-cancel','sequence-edit-close','sequence-editor-dialog','sequence-organize-open',
+  'sequence-organize-close','sequence-organize-done','sequence-order-reset','sequence-organize-dialog','sequence-record-dialog',
+  'sequence-record-close','sequence-record-cancel','sequence-record-done','sequence-record-play','sequence-record-section-select',
+  'sequence-record-pause','sequence-record-undo','sequence-record-clear-all'];
+ const elements={};
+ ids.forEach(function(id){elements[id]=fakeElement(id)});
+ const sectionButtons=[fakeElement('',{sequenceSection:'verse'}),fakeElement('',{sequenceSection:'chorus'})];
+ const repeatButtons=[fakeElement('',{repeatAdjust:'verse',delta:'-1'}),fakeElement('',{repeatAdjust:'verse',delta:'1'})];
+ const repeatDisplays=[fakeElement('',{sectionRepeatDisplay:'verse'}),fakeElement('',{sectionRepeatDisplay:'chorus'})];
+ const sectionControls=[fakeElement('',{sectionControl:'verse'}),fakeElement('',{sectionControl:'chorus'})];
+ const fractions=[fakeElement('',{recordFraction:'0.25'}),fakeElement('',{recordFraction:'1'})];
+ const document={
+  querySelectorAll:function(selector){
+   if(selector==='[data-sequence-section]')return sectionButtons;
+   if(selector==='[data-repeat-adjust]')return repeatButtons;
+   if(selector==='[data-section-repeat-display]')return repeatDisplays;
+   if(selector==='[data-section-control]')return sectionControls;
+   if(selector==='[data-record-fraction]')return fractions;
+   return [];
+  }
+ };
+ const calls=[];
+ const action=function(name){return function(){calls.push([name].concat(Array.prototype.slice.call(arguments)))}};
+ const options={
+  document:document,getElement:function(id){return elements[id]},selectSection:action('select'),adjustRepeat:action('repeat'),
+  addPause:action('pause'),togglePlayback:action('play'),toggleHoldLoop:action('loop'),saveSectionControls:action('section-controls'),
+  changeSectionPattern:action('pattern'),toggleDrums:action('drums'),toggleAuto:action('auto'),toggleAutoEnd:action('auto-end'),
+  deleteLast:action('delete-last'),clearAll:action('clear-all'),togglePanel:action('panel'),syncEditorKind:action('editor-kind'),
+  commitEditor:action('editor-commit'),closeEditor:action('editor-close'),confirmDelete:function(){calls.push(['confirm-delete']);return Promise.resolve(true)},
+  openOrganizer:action('organizer-open'),resetOrder:action('organizer-reset'),organizerDone:action('organizer-done'),
+  openRecordDialog:action('record-open'),cancelRecordDialog:action('record-cancel'),finishRecordDialog:action('record-done'),
+  toggleRecordPreview:action('record-preview'),selectRecordSection:action('record-section'),selectRecordFraction:action('record-fraction'),
+  addRecordPause:action('record-pause'),undoRecordItem:action('record-undo'),clearRecordItems:action('record-clear'),
+  recordDialogClosed:action('record-closed'),finishRecordPointer:action('record-pointer'),refreshRecordAfterEditor:action('record-refresh')
+ };
+ const window={document:document};
+ vm.runInNewContext(source,{window:window,globalThis:window},{filename:'sequencer.js'});
+ const controller=window.GeraSequencerUI.createController(options);
+ return{api:window.GeraSequencerUI,controller:controller,elements:elements,sectionButtons:sectionButtons,repeatButtons:repeatButtons,
+  repeatDisplays:repeatDisplays,sectionControls:sectionControls,fractions:fractions,calls:calls};
+}
+
+test('módulo expõe somente a fábrica do controlador do sequenciador',function(){
+ const h=harness();
+ assert.deepEqual(Object.keys(h.api),['createController']);
+ assert.equal(Object.isFrozen(h.api),true);
+});
+
+test('estados visuais preservam repetição, loop, Auto e visibilidade',function(){
+ const h=harness();
+ h.controller.syncRepeatInputs(function(section){return section==='verse'?2:0});
+ h.controller.syncHoldLoop(true);
+ h.controller.syncAuto(true,false);
+ h.controller.syncPanelVisibility(false);
+ assert.equal(h.repeatDisplays[0].textContent,'2');
+ assert.equal(h.repeatDisplays[1].title,'Fora do modo Auto');
+ assert.equal(h.elements['sequence-hold-loop'].textContent,'🔁 Em Loop');
+ assert.equal(h.elements['sequence-auto'].textContent,'Auto ✓');
+ assert.equal(h.elements['sequence-auto-end'].classList.contains('active'),false);
+ assert.equal(h.elements['sequence-panel'].classList.contains('sequence-hidden'),true);
+});
+
+test('botões de gravação, pausa, reprodução e bateria mantêm textos e ARIA',function(){
+ const h=harness();
+ h.controller.syncRecordButton(true);
+ h.controller.syncPauseButton({fraction:.5,fractionLabel:'½',durationLabel:'meio compasso'});
+ h.controller.syncPlayButton(true);
+ h.controller.syncDrumButton(true);
+ assert.equal(h.elements['sequence-record'].textContent,'■ Gravando');
+ assert.equal(h.elements['sequence-rest'].textContent,'⏸ Pausa ½');
+ assert.equal(h.elements['sequence-play'].attributes['aria-pressed'],'true');
+ assert.equal(h.elements['sequence-stop-drums'].attributes['aria-label'],'Parar bateria');
+});
+
+test('seções preservam seleção ativa, fila, conteúdo e repetição em execução',function(){
+ const h=harness();
+ h.controller.syncSectionButtons({labels:{verse:'A',chorus:'C'},activeSection:'verse',queuedSection:'chorus',playing:true,hasContent:function(section){return section==='verse'}});
+ assert.equal(h.sectionButtons[0].classList.contains('active-section'),true);
+ assert.equal(h.sectionButtons[0].classList.contains('has-content'),true);
+ assert.equal(h.sectionButtons[1].classList.contains('queued-section'),true);
+ assert.equal(h.sectionButtons[1].attributes['aria-label'],'C — próxima seção');
+ assert.equal(h.sectionControls[0].classList.contains('playing-repeat'),true);
+});
+
+test('liga os controles principais sem incorporar decisões musicais',function(){
+ const h=harness();
+ h.controller.bindMainControls();
+ h.sectionButtons[1].onclick();
+ h.repeatButtons[0].onclick();
+ h.elements['sequence-rest'].onclick();
+ h.elements['sequence-play'].onclick();
+ h.elements['section-drum-pattern'].value='rock';h.elements['section-drum-pattern'].onchange();
+ h.elements['sequence-auto'].onclick();
+ h.elements['sequence-clear'].onclick();
+ h.elements['sequence-toggle'].onclick();
+ assert.deepEqual(h.calls,[['select','chorus'],['repeat','verse',-1],['pause'],['play'],['pattern','rock'],['auto'],['delete-last'],['panel']]);
+});
+
+test('liga editor de item e organizador, inclusive confirmações e cancelamento',async function(){
+ const h=harness();
+ h.controller.bindItemEditor();
+ h.controller.bindOrganizer();
+ h.elements['sequence-edit-before'].onclick();
+ await h.elements['sequence-edit-delete'].onclick();
+ h.elements['sequence-editor-dialog'].listeners.cancel({preventDefault:function(){}});
+ h.elements['sequence-organize-open'].onclick();
+ h.elements['sequence-organize-done'].onclick();
+ assert.deepEqual(h.calls,[['editor-commit','before'],['editor-close'],['confirm-delete'],['editor-commit','delete'],['editor-close'],['organizer-open'],['organizer-done']]);
+ assert.equal(h.elements['sequence-organize-dialog'].closeCalls,1);
+});
+
+test('liga o diálogo de gravação e mantém ponteiros e atualização após edição',function(){
+ const h=harness();
+ h.controller.bindRecordDialog();
+ let prevented=0;
+ h.elements['sequence-record'].listeners.click({preventDefault:function(){prevented++},stopPropagation:function(){prevented++}});
+ h.fractions[0].onclick();
+ h.elements['sequence-record-pause'].onclick();
+ h.elements['sequence-record-dialog'].listeners.pointerup({pointerId:7});
+ h.elements['sequence-editor-dialog'].listeners.close();
+ assert.equal(prevented,2);
+ assert.deepEqual(h.calls,[['record-open'],['record-fraction',.25],['record-pause'],['record-pointer',{pointerId:7}],['record-refresh']]);
+});
+
+test('arquivo extraído não contém áudio, transporte, persistência ou estado musical',function(){
+ assert.doesNotMatch(source,/AudioContext|playChordSequence|startDrums|localStorage|GeraStorage|sequenceSections|sequencePlaying|sequenceAuto|setTimeout|setInterval|requestAnimationFrame/);
+});
+
+test('HTML e SERVICE WORKER carregam o módulo uma única vez na versão 3.15.26',function(){
+ const html=fs.readFileSync(path.join(root,'index.html'),'utf8');
+ const sw=fs.readFileSync(path.join(root,'sw.js'),'utf8');
+ const tag='<script src="./js/ui/sequencer.js"></script>';
+ assert.equal((html.match(/\.\/js\/ui\/sequencer\.js/g)||[]).length,1);
+ assert.ok(html.indexOf(tag)<html.indexOf('<script src="./js/audio/core.js"></script>'));
+ assert.equal((sw.match(/\.\/js\/ui\/sequencer\.js/g)||[]).length,1);
+ assert.ok(sw.includes("const CACHE_NAME = CACHE_PREFIX + 'v3.15.26';"));
+ assert.equal(JSON.parse(fs.readFileSync(path.join(root,'manifest.json'),'utf8')).version,'3.15.26');
+});
+
+test('módulo da 8F é a única adição funcional sobre a versão 3.15.25',function(){
+ const previous=path.resolve(root,'..','GERA-PWA-v3.15.25-etapa-8E-bateria');
+ const currentFiles=fs.readdirSync(path.join(root,'js','ui')).sort();
+ const previousFiles=fs.readdirSync(path.join(previous,'js','ui')).sort();
+ assert.deepEqual(currentFiles.filter(function(file){return previousFiles.indexOf(file)<0}),['sequencer.js']);
+});
+
+test('reversão exclusiva da 8F recompõe a versão 3.15.25 byte a byte',function(){
+ const previous=path.resolve(root,'..','GERA-PWA-v3.15.25-etapa-8E-bateria');
+ const previousIndex=fs.readFileSync(path.join(previous,'index.html'),'utf8');
+ let reconstructed=fs.readFileSync(path.join(root,'index.html'),'utf8');
+
+ function replaceRange(current,start,end,original){
+  const a=current.indexOf(start);const b=current.indexOf(end,a);
+  const c=original.indexOf(start);const d=original.indexOf(end,c);
+  assert.ok(a>=0&&b>a&&c>=0&&d>c,start);
+  return current.slice(0,a)+original.slice(c,d)+current.slice(b);
+ }
+
+ reconstructed=replaceRange(reconstructed,'function syncSequenceRepeatInputs(){','function clearChordLatchState',previousIndex);
+ reconstructed=replaceRange(reconstructed,'function syncSequencePlayButton(){','function playChordSequence(){',previousIndex);
+ reconstructed=replaceRange(reconstructed,"$('change-full').onclick=function(){selectChordChangeMode('nextBar')};","$('advanced-toggle').onclick=",previousIndex);
+ reconstructed=replaceRange(reconstructed,"const appConfirmOk=$('app-confirm-ok');","$('songs-open').onclick=",previousIndex);
+ reconstructed=replaceRange(reconstructed,' function bindSequenceRecordDialog(){',' function assemble(){',previousIndex);
+ reconstructed=reconstructed.replaceAll('3.15.26','3.15.25').replace('<script src="./js/ui/sequencer.js"></script>\n','');
+
+ const reconstructedSw=fs.readFileSync(path.join(root,'sw.js'),'utf8')
+  .replace("'v3.15.26'","'v3.15.25'")
+  .replace('    "./js/ui/sequencer.js",\n','');
+ const reconstructedManifest=fs.readFileSync(path.join(root,'manifest.json'),'utf8').replace('"3.15.26"','"3.15.25"');
+ assert.equal(reconstructed,previousIndex);
+ assert.equal(reconstructedSw,fs.readFileSync(path.join(previous,'sw.js'),'utf8'));
+ assert.equal(reconstructedManifest,fs.readFileSync(path.join(previous,'manifest.json'),'utf8'));
+});
diff --git a/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.25-etapa-8E-bateria/tests/tempo.test.js b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/tests/tempo.test.js
index a2a3da3..b2984dd 100644
--- a/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.25-etapa-8E-bateria/tests/tempo.test.js
+++ b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/tests/tempo.test.js
@@ -191,7 +191,7 @@ test('preserva o arquivo de BPM no SERVICE WORKER e atualiza somente a versão v
  const currentSw=fs.readFileSync(path.join(root,'sw.js'),'utf8');
  assert.ok(currentSw.includes('    "./js/transport/tempo.js",'));
  const currentManifest=JSON.parse(fs.readFileSync(path.join(root,'manifest.json'),'utf8'));
- assert.equal(currentManifest.version,'3.15.25');
+ assert.equal(currentManifest.version,'3.15.26');
 });
 
 test('todos os demais recursos funcionais permanecem byte a byte iguais',function(){
diff --git a/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.25-etapa-8E-bateria/tests/transport-inventory.test.js b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/tests/transport-inventory.test.js
index 38d481a..615418a 100644
--- a/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.25-etapa-8E-bateria/tests/transport-inventory.test.js
+++ b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/tests/transport-inventory.test.js
@@ -90,7 +90,7 @@ test('painel e contadores mantêm atualização de 250 ms',function(){
 
 test('SERVICE WORKER atualiza o cache e mantém a limpeza de versões antigas',function(){
  assert.ok(sw.includes("const CACHE_PREFIX = 'gera-pwa-';"));
- assert.ok(sw.includes("const CACHE_NAME = CACHE_PREFIX + 'v3.15.25';"));
+ assert.ok(sw.includes("const CACHE_NAME = CACHE_PREFIX + 'v3.15.26';"));
  assert.match(sw,/caches\.keys\(\).*cacheName !== CACHE_NAME.*caches\.delete\(cacheName\)/s);
 });
 
diff --git a/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.25-etapa-8E-bateria/tests/transport-status.test.js b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/tests/transport-status.test.js
index 24a157f..099644b 100644
--- a/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.25-etapa-8E-bateria/tests/transport-status.test.js
+++ b/workspace/scratch/9f3302d64176/GERA-PWA-v3.15.26-etapa-8F-sequenciador/tests/transport-status.test.js
@@ -176,6 +176,6 @@ test('módulos de interface extraídos permanecem no pré-cache',function(){
  assert.ok(sw.includes('    "./js/ui/transport-status.js",'));
  assert.ok(sw.includes('    "./js/ui/compact-panel.js",'));
  assert.ok(sw.includes('    "./js/ui/keyboard.js",'));
- assert.ok(sw.includes("const CACHE_NAME = CACHE_PREFIX + 'v3.15.25';"));
- assert.equal(JSON.parse(fs.readFileSync(path.join(root,'manifest.json'),'utf8')).version,'3.15.25');
+ assert.ok(sw.includes("const CACHE_NAME = CACHE_PREFIX + 'v3.15.26';"));
+ assert.equal(JSON.parse(fs.readFileSync(path.join(root,'manifest.json'),'utf8')).version,'3.15.26');
 });

