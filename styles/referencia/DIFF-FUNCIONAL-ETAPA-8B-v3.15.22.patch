--- a/index.html
+++ b/index.html
@@ -15,7 +15,7 @@
 <link rel="icon" type="image/png" sizes="512x512" href="./icons/icon-512.png">
 <link rel="apple-touch-icon" href="./icons/icon-192.png">
 <meta name="screen-orientation" content="landscape">
-<title>GERA v3.15.21</title>
+<title>GERA v3.15.22</title>
 <script src="./js/storage.js"></script>
 <script>
 (function(){
@@ -38,7 +38,7 @@
 <div id="rotate-msg"><div class="icon">📱</div><h2>Gire o dispositivo</h2><p>O GERA foi organizado para uso horizontal.</p></div>
 <header class="app-header">
   <div class="header-brand">
-    <h1><span class="gera-brand-mark" aria-hidden="true"><img src="icons/icon-192.png" alt=""></span><span class="gera-title">GERA</span><span class="gera-subtitle">Gerador de Acompanhamentos</span><small class="app-version">v3.15.21</small></h1>
+    <h1><span class="gera-brand-mark" aria-hidden="true"><img src="icons/icon-192.png" alt=""></span><span class="gera-title">GERA</span><span class="gera-subtitle">Gerador de Acompanhamentos</span><small class="app-version">v3.15.22</small></h1>
   </div>
   <div class="header-actions">
     <button type="button" id="theme-cycle" class="secondary compact-theme-button" aria-label="Alterar tema do aplicativo" title="Alterar tema">🎨 Escuro</button>
@@ -163,7 +163,7 @@
   <div class="redesign-brandmark">
     <span class="redesign-logo">GERA</span>
     <span class="redesign-tagline">Gerador de Acompanhamentos</span>
-    <small>v3.15.21</small>
+    <small>v3.15.22</small>
   </div>
   <button class="redesign-song-pill" id="redesign-song-pill" type="button" title="Escolher música">♫ <span id="redesign-song-name">Nenhuma música</span></button>
   <div id="redesign-header-text-preview" class="sequence-text-preview sequence-text-preview-floating" aria-live="polite">
@@ -815,6 +815,7 @@
 <script src="./js/transport/coordinator.js"></script>
 <script src="./js/ui/transport-status.js"></script>
 <script src="./js/ui/header.js"></script>
+<script src="./js/ui/compact-panel.js"></script>
 <script src="./js/audio/core.js"></script>
 <script>
 "use strict";
@@ -6996,84 +6997,59 @@
 refreshMemoryButtons();
 document.addEventListener('keydown',e=>{if(['INPUT','SELECT','BUTTON'].includes(e.target.tagName))return;const k=e.key.toLowerCase();if(pressedKeys.has(k)||!KEYMAP.has(k))return;noteDown(KEYMAP.get(k),k,1,'keyboard')});document.addEventListener('keyup',e=>{const k=e.key.toLowerCase();if(KEYMAP.has(k))noteUp(KEYMAP.get(k),k,'keyboard')});window.addEventListener('blur',()=>releaseAll());document.addEventListener('contextmenu',e=>{if(e.target.closest('.key,.chord'))e.preventDefault()});
 initializeGeraTheme();
-const compactModeToggle=$('compact-mode-toggle');
-const compactModeClose=$('compact-mode-close');
-const compactSongsOpen=$('compact-songs-open');
-const compactPlayButton=$('compact-play');
-const compactSequenceOnlyButton=$('compact-sequence-only');
-const compactLoop=$('compact-loop');
-const compactDrumOnly=$('compact-drum-only');
-const compactDrumFill=$('compact-drum-fill');
-const compactDrumEnding=$('compact-drum-ending');
-const compactCapoDown=$('compact-capo-down');
-const compactCapoUp=$('compact-capo-up');
-const compactOctaveDown=$('compact-octave-down');
-const compactOctaveUp=$('compact-octave-up');
-const compactCircleRoot=$('compact-circle-root');
-const compactListSelect=$('compact-list-select');
-const compactPrevSong=$('compact-prev-song');
-const compactNextSong=$('compact-next-song');
-const compactTransitionModeSelect=$('compact-transition-mode');
-const compactNextStartModeSelect=$('compact-next-start-mode');
-const compactListEndModeSelect=$('compact-list-end-mode');
-const compactCarouselPrev=$('compact-carousel-prev');
-const compactCarouselNext=$('compact-carousel-next');
-
-if(compactModeToggle)compactModeToggle.onclick=function(){setCompactMode(!compactModeActive)};
-if(compactModeClose)compactModeClose.onclick=function(){setCompactMode(false)};
-if(compactSongsOpen)compactSongsOpen.onclick=function(){
- renderSongsList();
- $('song-name-input').value=currentSongName||'';
- $('song-bpm-input').value=bpm;
- $('songs-dialog').showModal();
-};
-if(compactPlayButton)compactPlayButton.onclick=compactPlayStandard;
-if(compactSequenceOnlyButton)compactSequenceOnlyButton.onclick=compactPlaySequenceOnly;
-if(compactLoop)compactLoop.onclick=compactToggleLoop;
-if(compactDrumOnly)compactDrumOnly.onclick=compactToggleDrums;
-if(compactDrumFill)compactDrumFill.onclick=function(){requestDrumAction('fill')};
-if(compactDrumEnding)compactDrumEnding.onclick=function(){requestDrumAction('ending')};
-if(compactCapoDown)compactCapoDown.onclick=function(){compactAdjustCapo(-1)};
-if(compactCapoUp)compactCapoUp.onclick=function(){compactAdjustCapo(1)};
-if(compactOctaveDown)compactOctaveDown.onclick=function(){compactAdjustOctave(-1)};
-if(compactOctaveUp)compactOctaveUp.onclick=function(){compactAdjustOctave(1)};
-if(compactCircleRoot)compactCircleRoot.onchange=function(){
- circleRoot=Number(this.value);
- circleEnabled=true;
- renderCompactHarmonicChords();
-};
-if(compactListSelect)compactListSelect.onchange=function(){
- activeSongListId=this.value;
- compactListIndex=-1;
- const names=compactListSongNames();
- if(names.length){
-  compactLoadListIndex(0,'wait');
-  setStatus('Primeira música da lista selecionada: "'+names[0]+'"');
- }else{
+GeraCompactPanel.createController({
+ getElement:function(id){return $(id)},
+ toggleMode:function(){setCompactMode(!compactModeActive)},
+ closeMode:function(){setCompactMode(false)},
+ openSongs:function(){
+  renderSongsList();
+  $('song-name-input').value=currentSongName||'';
+  $('song-bpm-input').value=bpm;
+  $('songs-dialog').showModal();
+ },
+ playStandard:compactPlayStandard,
+ playSequenceOnly:compactPlaySequenceOnly,
+ toggleLoop:compactToggleLoop,
+ toggleDrums:compactToggleDrums,
+ requestDrumAction:requestDrumAction,
+ adjustCapo:compactAdjustCapo,
+ adjustOctave:compactAdjustOctave,
+ changeCircleRoot:function(value){
+  circleRoot=Number(value);
+  circleEnabled=true;
+  renderCompactHarmonicChords();
+ },
+ changeList:function(value){
+  activeSongListId=value;
+  compactListIndex=-1;
+  const names=compactListSongNames();
+  if(names.length){
+   compactLoadListIndex(0,'wait');
+   setStatus('Primeira música da lista selecionada: "'+names[0]+'"');
+  }else{
+   saveSongLists();
+   syncCompactControls();
+   if(activeSongListId)setStatus('A lista selecionada não possui músicas');
+  }
+ },
+ goRelative:function(delta){compactGoRelative(delta,compactNextStartMode)},
+ changeTransitionMode:function(value){
+  compactTransitionMode=value==='auto'?'auto':'manual';
   saveSongLists();
   syncCompactControls();
-  if(activeSongListId)setStatus('A lista selecionada não possui músicas');
- }
-};
-if(compactPrevSong)compactPrevSong.onclick=function(){compactGoRelative(-1,compactNextStartMode)};
-if(compactNextSong)compactNextSong.onclick=function(){compactGoRelative(1,compactNextStartMode)};
-if(compactTransitionModeSelect)compactTransitionModeSelect.onchange=function(){
- compactTransitionMode=this.value==='auto'?'auto':'manual';
- saveSongLists();
- syncCompactControls();
-};
-if(compactNextStartModeSelect)compactNextStartModeSelect.onchange=function(){
- compactNextStartMode=this.value==='play'?'play':'wait';
- saveSongLists();
- syncCompactControls();
-};
-if(compactListEndModeSelect)compactListEndModeSelect.onchange=function(){
- compactListEndMode=this.value==='loop'?'loop':'stop';
- saveSongLists();
- syncCompactControls();
-};
-if(compactCarouselPrev)compactCarouselPrev.onclick=function(){scrollCompactCarousel(-1)};
-if(compactCarouselNext)compactCarouselNext.onclick=function(){scrollCompactCarousel(1)};
+ },
+ changeNextStartMode:function(value){
+  compactNextStartMode=value==='play'?'play':'wait';
+  saveSongLists();
+  syncCompactControls();
+ },
+ changeListEndMode:function(value){
+  compactListEndMode=value==='loop'?'loop':'stop';
+  saveSongLists();
+  syncCompactControls();
+ },
+ scrollCarousel:scrollCompactCarousel
+}).bind();
 syncGlobalMuteUI();
 const globalMuteButton=$('global-mute-float');
 if(globalMuteButton)globalMuteButton.onclick=toggleGlobalAudioMute;
--- a/sw.js
+++ b/sw.js
@@ -2,7 +2,7 @@
 
 const CACHE_PREFIX = 'gera-pwa-';
 const LEGACY_CACHE_PREFIX = 'teclado-virtual-pwa-';
-const CACHE_NAME = CACHE_PREFIX + 'v3.15.21';
+const CACHE_NAME = CACHE_PREFIX + 'v3.15.22';
 const OFFLINE_URL = './offline.html';
 
 const PRECACHE_URLS = [
@@ -24,6 +24,7 @@
     "./js/transport/coordinator.js",
     "./js/ui/transport-status.js",
     "./js/ui/header.js",
+    "./js/ui/compact-panel.js",
     "./js/audio/core.js",
     "./kit-acustico-selecionado/MAPEAMENTO.txt",
     "./kit-acustico-selecionado/cymbals/crash-1.wav",
--- a/manifest.json
+++ b/manifest.json
@@ -39,5 +39,5 @@
             "purpose": "maskable"
         }
     ],
-    "version": "3.15.21"
+    "version": "3.15.22"
 }
--- /dev/null
+++ b/js/ui/compact-panel.js
@@ -0,0 +1,49 @@
+/**
+ * Controlador dos comandos do painel compacto do GERA.
+ * Preserva o DOM existente e encaminha cada ação ao núcleo responsável.
+ */
+(function(global){
+ 'use strict';
+
+ function createController(options){
+  const getElement=options.getElement;
+
+  function element(id){return getElement(id)}
+  function bindClick(id,handler){
+   const control=element(id);
+   if(control)control.onclick=handler;
+  }
+  function bindChange(id,handler){
+   const control=element(id);
+   if(control)control.onchange=function(){handler(this.value,this)};
+  }
+  function bind(){
+   bindClick('compact-mode-toggle',options.toggleMode);
+   bindClick('compact-mode-close',options.closeMode);
+   bindClick('compact-songs-open',options.openSongs);
+   bindClick('compact-play',options.playStandard);
+   bindClick('compact-sequence-only',options.playSequenceOnly);
+   bindClick('compact-loop',options.toggleLoop);
+   bindClick('compact-drum-only',options.toggleDrums);
+   bindClick('compact-drum-fill',function(){options.requestDrumAction('fill')});
+   bindClick('compact-drum-ending',function(){options.requestDrumAction('ending')});
+   bindClick('compact-capo-down',function(){options.adjustCapo(-1)});
+   bindClick('compact-capo-up',function(){options.adjustCapo(1)});
+   bindClick('compact-octave-down',function(){options.adjustOctave(-1)});
+   bindClick('compact-octave-up',function(){options.adjustOctave(1)});
+   bindChange('compact-circle-root',options.changeCircleRoot);
+   bindChange('compact-list-select',options.changeList);
+   bindClick('compact-prev-song',function(){options.goRelative(-1)});
+   bindClick('compact-next-song',function(){options.goRelative(1)});
+   bindChange('compact-transition-mode',options.changeTransitionMode);
+   bindChange('compact-next-start-mode',options.changeNextStartMode);
+   bindChange('compact-list-end-mode',options.changeListEndMode);
+   bindClick('compact-carousel-prev',function(){options.scrollCarousel(-1)});
+   bindClick('compact-carousel-next',function(){options.scrollCarousel(1)});
+  }
+
+  return Object.freeze({bind:bind});
+ }
+
+ global.GeraCompactPanel=Object.freeze({createController:createController});
+})(window);
