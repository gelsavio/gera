--- a/index.html
+++ b/index.html
@@ -15,7 +15,7 @@
 <link rel="icon" type="image/png" sizes="512x512" href="./icons/icon-512.png">
 <link rel="apple-touch-icon" href="./icons/icon-192.png">
 <meta name="screen-orientation" content="landscape">
-<title>GERA v3.15.26</title>
+<title>GERA v3.15.27</title>
 <script src="./js/storage.js"></script>
 <script>
 (function(){
@@ -38,7 +38,7 @@
 <div id="rotate-msg"><div class="icon">📱</div><h2>Gire o dispositivo</h2><p>O GERA foi organizado para uso horizontal.</p></div>
 <header class="app-header">
   <div class="header-brand">
-    <h1><span class="gera-brand-mark" aria-hidden="true"><img src="icons/icon-192.png" alt=""></span><span class="gera-title">GERA</span><span class="gera-subtitle">Gerador de Acompanhamentos</span><small class="app-version">v3.15.26</small></h1>
+    <h1><span class="gera-brand-mark" aria-hidden="true"><img src="icons/icon-192.png" alt=""></span><span class="gera-title">GERA</span><span class="gera-subtitle">Gerador de Acompanhamentos</span><small class="app-version">v3.15.27</small></h1>
   </div>
   <div class="header-actions">
     <button type="button" id="theme-cycle" class="secondary compact-theme-button" aria-label="Alterar tema do aplicativo" title="Alterar tema">🎨 Escuro</button>
@@ -163,7 +163,7 @@
   <div class="redesign-brandmark">
     <span class="redesign-logo">GERA</span>
     <span class="redesign-tagline">Gerador de Acompanhamentos</span>
-    <small>v3.15.26</small>
+    <small>v3.15.27</small>
   </div>
   <button class="redesign-song-pill" id="redesign-song-pill" type="button" title="Escolher música">♫ <span id="redesign-song-name">Nenhuma música</span></button>
   <div id="redesign-header-text-preview" class="sequence-text-preview sequence-text-preview-floating" aria-live="polite">
@@ -820,6 +820,7 @@
 <script src="./js/ui/chords-circle.js"></script>
 <script src="./js/ui/drums.js"></script>
 <script src="./js/ui/sequencer.js"></script>
+<script src="./js/ui/songs-library.js"></script>
 <script src="./js/audio/core.js"></script>
 <script>
 "use strict";
@@ -2789,125 +2790,22 @@
  openSongListEditor(id);
 }
 function renderSongListsLibrary(){
- const box=$('song-lists-list');
- if(!box)return;
- box.innerHTML='';
- const ids=Object.keys(songLists).sort(function(a,b){
-  return songLists[a].name.localeCompare(songLists[b].name,'pt-BR');
- });
- if(!ids.length){
-  box.innerHTML='<span class="songs-empty">Nenhuma lista criada.</span>';
-  return;
- }
- ids.forEach(function(id){
-  const list=songLists[id];
-  const row=document.createElement('div');
-  row.className='song-list-library-item';
-
-  const open=document.createElement('button');
-  open.type='button';
-  open.className='secondary song-list-open';
-  open.innerHTML='<strong></strong><small></small>';
-  open.querySelector('strong').textContent=list.name;
-  open.querySelector('small').textContent=list.songNames.length+(list.songNames.length===1?' música':' músicas');
-  open.onclick=function(){openSongListEditor(id)};
-
-  const use=document.createElement('button');
-  use.type='button';
-  use.className='secondary';
-  use.textContent='Usar';
-  use.title='Selecionar esta lista no modo compacto';
-  use.onclick=function(){
-   activeSongListId=id;
-   compactListIndex=list.songNames.indexOf(currentSongName);
-   if(compactListIndex<0)compactListIndex=list.songNames.length?0:-1;
-   saveSongLists();
-   syncCompactListControls();
-   setStatus('Lista "'+list.name+'" selecionada');
-  };
-
-  row.append(open,use);
-  box.appendChild(row);
- });
+ songsLibraryUI.renderSongListsLibrary(songLists,currentSongName);
 }
 function openSongListEditor(id){
  if(!songLists[id])return;
  songListEditorId=id;
  songListEditorDraft=JSON.parse(JSON.stringify(songLists[id]));
- $('song-list-editor-name').value=songListEditorDraft.name;
+ songsLibraryUI.openSongListDialog(songListEditorDraft.name);
  renderSongListEditor();
- const dialog=$('song-list-editor-dialog');
- if(dialog&&!dialog.open)dialog.showModal();
 }
 function closeSongListEditor(){
  songListEditorId='';
  songListEditorDraft=null;
- const dialog=$('song-list-editor-dialog');
- if(dialog&&dialog.open)dialog.close();
+ songsLibraryUI.closeSongListDialog();
 }
 function renderSongListEditor(){
- if(!songListEditorDraft)return;
- $('song-list-editor-title').textContent='Editar lista';
- const available=$('song-list-available');
- const order=$('song-list-order');
- available.innerHTML='';
- order.innerHTML='';
-
- Object.keys(songs).sort(function(a,b){return a.localeCompare(b,'pt-BR')}).forEach(function(name){
-  const label=document.createElement('label');
-  label.className='song-list-check';
-  const check=document.createElement('input');
-  check.type='checkbox';
-  check.checked=songListEditorDraft.songNames.indexOf(name)>=0;
-  check.onchange=function(){
-   const position=songListEditorDraft.songNames.indexOf(name);
-   if(this.checked&&position<0)songListEditorDraft.songNames.push(name);
-   if(!this.checked&&position>=0)songListEditorDraft.songNames.splice(position,1);
-   renderSongListEditor();
-  };
-  const span=document.createElement('span');
-  span.textContent=name;
-  label.append(check,span);
-  available.appendChild(label);
- });
-
- if(!songListEditorDraft.songNames.length){
-  order.innerHTML='<span class="songs-empty">Nenhuma música adicionada.</span>';
- }else{
-  songListEditorDraft.songNames.forEach(function(name,index){
-   const row=document.createElement('div');
-   row.className='song-list-order-item';
-   const number=document.createElement('span');
-   number.textContent=String(index+1);
-   const title=document.createElement('strong');
-   title.textContent=name;
-
-   const up=document.createElement('button');
-   up.type='button';up.textContent='↑';up.disabled=index===0;
-   up.onclick=function(){
-    const other=songListEditorDraft.songNames[index-1];
-    songListEditorDraft.songNames[index-1]=name;
-    songListEditorDraft.songNames[index]=other;
-    renderSongListEditor();
-   };
-   const down=document.createElement('button');
-   down.type='button';down.textContent='↓';down.disabled=index===songListEditorDraft.songNames.length-1;
-   down.onclick=function(){
-    const other=songListEditorDraft.songNames[index+1];
-    songListEditorDraft.songNames[index+1]=name;
-    songListEditorDraft.songNames[index]=other;
-    renderSongListEditor();
-   };
-   const remove=document.createElement('button');
-   remove.type='button';remove.className='danger';remove.textContent='×';
-   remove.onclick=function(){
-    songListEditorDraft.songNames.splice(index,1);
-    renderSongListEditor();
-   };
-   row.append(number,title,up,down,remove);
-   order.appendChild(row);
-  });
- }
+ songsLibraryUI.renderSongListEditor(songListEditorDraft,Object.keys(songs));
 }
 function saveSongListEditor(){
  if(!songListEditorDraft||!songLists[songListEditorId])return;
@@ -3448,74 +3346,14 @@
  syncSequenceSectionButtons();renderChordSequence();syncSectionDrumControls();const capoButton=$('capo-label');if(capoButton)capoButton.textContent=capoLabel();saveChordSequence();syncInstrumentChangeLock();
 }
 function updateSongNameDisplay(){
- const el=$('song-current-name');
- if(!el)return;
- el.textContent=currentSongName?'🎵 '+currentSongName:'Nenhuma música selecionada';
- el.title=currentSongName||'Nenhuma música selecionada';
+ songsLibraryUI.updateSongNameDisplay(currentSongName);
 }
 function renderSongsList(){
- const box=$('songs-list');
- if(!box)return;
- const names=Object.keys(songs).sort(function(a,b){return a.localeCompare(b,'pt-BR')});
- renderSongListsLibrary();
-
- if(!names.length){
-  box.innerHTML='<span class="songs-empty">Nenhuma música salva ainda.</span>';
-  return;
- }
-
- box.innerHTML='';
- names.forEach(function(name){
-  const row=document.createElement('div');
-  row.className='song-item'+(name===currentSongName?' song-active':'');
-
-  const info=document.createElement('div');
-  info.className='song-library-info';
-
-  const loadBtn=document.createElement('button');
-  loadBtn.type='button';
-  loadBtn.className='song-load secondary';
-  loadBtn.textContent=name;
-  loadBtn.title='Carregar "'+name+'"';
-  loadBtn.onclick=function(){loadSong(name)};
-
-  const memberships=document.createElement('small');
-  const listNames=songListsForSong(name).map(function(id){return songLists[id].name});
-  memberships.textContent=listNames.length?listNames.join(' · '):'Fora de listas';
-
-  info.append(loadBtn,memberships);
-
-  const listBtn=document.createElement('button');
-  listBtn.type='button';
-  listBtn.className='secondary song-list-membership';
-  listBtn.textContent='Listas';
-  listBtn.title='Adicionar ou remover esta música das listas';
-  listBtn.onclick=function(){
-   const firstList=songListsForSong(name)[0];
-   if(firstList){
-    openSongListEditor(firstList);
-    return;
-   }
-   const ids=Object.keys(songLists);
-   if(!ids.length){
-    const input=$('song-list-new-name');
-    if(input)input.focus();
-    setStatus('Crie uma lista para adicionar a música');
-    return;
-   }
-   openSongListEditor(ids[0]);
-  };
-
-  const delBtn=document.createElement('button');
-  delBtn.type='button';
-  delBtn.className='song-delete danger';
-  delBtn.textContent='🗑';
-  delBtn.title='Excluir "'+name+'"';
-  delBtn.setAttribute('aria-label','Excluir '+name);
-  delBtn.onclick=function(){deleteSong(name)};
-
-  row.append(info,listBtn,delBtn);
-  box.appendChild(row);
+ songsLibraryUI.renderSongsList({
+  songs:songs,
+  songLists:songLists,
+  currentSongName:currentSongName,
+  memberships:function(name){return songListsForSong(name).map(function(id){return songLists[id].name})}
  });
 }
 function prepareSongWorkspaceChange(){
@@ -3571,6 +3409,76 @@
  if(currentSongName===name)currentSongName=null;
  saveSongsStore();pruneSongLists();renderSongsList();updateSongNameDisplay();syncCompactListControls();
 }
+function manageSongListsForSong(name){
+ const firstList=songListsForSong(name)[0];
+ if(firstList){openSongListEditor(firstList);return}
+ const ids=Object.keys(songLists);
+ if(!ids.length){
+  const input=$('song-list-new-name');
+  if(input)input.focus();
+  setStatus('Crie uma lista para adicionar a música');
+  return;
+ }
+ openSongListEditor(ids[0]);
+}
+async function saveSongFromDialog(){
+ const input=$('song-name-input'),name=(input.value||'').trim();
+ if(!name){setStatus('Digite um nome para a música');return}
+ const overwrite=!!songs[name]&&name!==currentSongName;
+ if(overwrite&&!(await appConfirm('Já existe uma música chamada "'+name+'". Substituir?','Substituir música')))return;
+ const songBpm=clampFineInput($('song-bpm-input'));
+ requestBpmChange(songBpm);
+ songs[name]={...snapshotCurrentSequence(),bpm:songBpm};currentSongName=name;saveSongsStore();renderSongsList();updateSongNameDisplay();syncCompactListControls();
+ syncInstrumentChangeLock();
+ setStatus('Música "'+name+'" salva a '+songBpm+' BPM');
+}
+async function startBlankSongFromDialog(){
+ if(!(await appConfirm('Começar uma música em branco? As A a P na tela serão limpas. A música atual, se já tiver nome salvo, continuará guardada.','Nova música')))return;
+ prepareSongWorkspaceChange();
+ applySequenceState({sections:{verse:[],prechorus:[],chorus:[],bridge:[],section5:[],section6:[],section7:[],section8:[],section9:[],section10:[],section11:[],section12:[],section13:[],section14:[],section15:[],section16:[]},repeats:{verse:0,prechorus:0,chorus:0,bridge:0,section5:0,section6:0,section7:0,section8:0,section9:0,section10:0,section11:0,section12:0,section13:0,section14:0,section15:0,section16:0}});
+ currentSongName=null;saveSongsStore();updateSongNameDisplay();
+ const input=$('song-name-input');if(input)input.value='';
+ setStatus('Nova música em branco pronta para gravar');
+}
+const songsLibraryUI=GeraSongsLibraryUI.createController({
+ document:document,
+ getElement:function(id){return $(id)},
+ openSongList:openSongListEditor,
+ useSongList:function(id,currentIndex){
+  activeSongListId=id;
+  compactListIndex=currentIndex>=0?currentIndex:(songLists[id].songNames.length?0:-1);
+  saveSongLists();syncCompactListControls();setStatus('Lista "'+songLists[id].name+'" selecionada');
+ },
+ toggleDraftSong:function(name,checked){
+  const position=songListEditorDraft.songNames.indexOf(name);
+  if(checked&&position<0)songListEditorDraft.songNames.push(name);
+  if(!checked&&position>=0)songListEditorDraft.songNames.splice(position,1);
+  renderSongListEditor();
+ },
+ moveDraftSong:function(index,delta){
+  const target=index+delta;
+  const name=songListEditorDraft.songNames[index];
+  const other=songListEditorDraft.songNames[target];
+  songListEditorDraft.songNames[target]=name;
+  songListEditorDraft.songNames[index]=other;
+  renderSongListEditor();
+ },
+ removeDraftSong:function(index){songListEditorDraft.songNames.splice(index,1);renderSongListEditor()},
+ loadSong:loadSong,
+ manageSongLists:manageSongListsForSong,
+ deleteSong:deleteSong,
+ renderSongs:renderSongsList,
+ exportSong:exportCurrentSong,
+ chooseImport:chooseSongImportFile,
+ importFile:handleSongImportFile,
+ openSongs:function(){songsLibraryUI.openSongsDialog(currentSongName,bpm)},
+ saveSong:saveSongFromDialog,
+ newSong:startBlankSongFromDialog,
+ createSongList:createSongList,
+ closeSongList:closeSongListEditor,
+ saveSongList:saveSongListEditor,
+ deleteSongList:deleteSongListEditor
+});
 const MAX_SEQUENCE_REPEATS=99;
 function sectionRepeatValue(section){
  const value=Number(sequenceRepeats[section]);
@@ -6569,9 +6477,6 @@
  saveChordSequence();
  syncSectionDrumControls();
 }
-$('song-export').onclick=exportCurrentSong;
-$('song-import').onclick=chooseSongImportFile;
-$('song-import-file').onchange=handleSongImportFile;
 function toggleSequenceAuto(){
  sequenceAuto=!sequenceAuto;
  if(sequenceAuto)sequenceAutoEnd=false;
@@ -6862,47 +6767,8 @@
 if(appConfirmCancel)appConfirmCancel.onclick=function(){closeAppConfirm(false)};
 if(appConfirmDialog)appConfirmDialog.addEventListener('cancel',function(event){event.preventDefault();closeAppConfirm(false)});
 sequencerUI.bindItemEditor();
-const songListCreateButton=$('song-list-create');
-const songListEditorClose=$('song-list-editor-close');
-const songListEditorCancel=$('song-list-editor-cancel');
-const songListEditorSave=$('song-list-editor-save');
-const songListDelete=$('song-list-delete');
-const songListEditorDialog=$('song-list-editor-dialog');
-if(songListCreateButton)songListCreateButton.onclick=function(){
- createSongList($('song-list-new-name').value);
- $('song-list-new-name').value='';
-};
-if(songListEditorClose)songListEditorClose.onclick=closeSongListEditor;
-if(songListEditorCancel)songListEditorCancel.onclick=closeSongListEditor;
-if(songListEditorSave)songListEditorSave.onclick=saveSongListEditor;
-if(songListDelete)songListDelete.onclick=deleteSongListEditor;
-if(songListEditorDialog)songListEditorDialog.addEventListener('cancel',function(event){
- event.preventDefault();
- closeSongListEditor();
-});
-
+songsLibraryUI.bindMainControls();
 sequencerUI.bindOrganizer();
-$('songs-open').onclick=()=>{renderSongsList();$('song-name-input').value=currentSongName||'';$('song-bpm-input').value=bpm;$('songs-dialog').showModal()};
-$('songs-close').onclick=()=>$('songs-dialog').close();
-$('song-save').onclick=async ()=>{
- const input=$('song-name-input'),name=(input.value||'').trim();
- if(!name){setStatus('Digite um nome para a música');return}
- const overwrite=!!songs[name]&&name!==currentSongName;
- if(overwrite&&!(await appConfirm('Já existe uma música chamada "'+name+'". Substituir?','Substituir música')))return;
- const songBpm=clampFineInput($('song-bpm-input'));
- requestBpmChange(songBpm);
- songs[name]={...snapshotCurrentSequence(),bpm:songBpm};currentSongName=name;saveSongsStore();renderSongsList();updateSongNameDisplay();syncCompactListControls();
- syncInstrumentChangeLock();
- setStatus('Música "'+name+'" salva a '+songBpm+' BPM');
-};
-$('song-new').onclick=async ()=>{
- if(!(await appConfirm('Começar uma música em branco? As A a P na tela serão limpas. A música atual, se já tiver nome salvo, continuará guardada.','Nova música')))return;
- prepareSongWorkspaceChange();
- applySequenceState({sections:{verse:[],prechorus:[],chorus:[],bridge:[],section5:[],section6:[],section7:[],section8:[],section9:[],section10:[],section11:[],section12:[],section13:[],section14:[],section15:[],section16:[]},repeats:{verse:0,prechorus:0,chorus:0,bridge:0,section5:0,section6:0,section7:0,section8:0,section9:0,section10:0,section11:0,section12:0,section13:0,section14:0,section15:0,section16:0}});
- currentSongName=null;saveSongsStore();updateSongNameDisplay();
- const input=$('song-name-input');if(input)input.value='';
- setStatus('Nova música em branco pronta para gravar');
-};
 </script>
 
 <script>
@@ -6947,22 +6813,28 @@
 
  let songSignature='';
  let redesignBpmSongName='';
+ const redesignSongsUI=GeraSongsLibraryUI.createController({
+  document:document,
+  getElement:byId,
+  renderRedesignSongs:function(){renderRedesignSongs()},
+  closeSongBpm:function(){closeRedesignSongBpmDialog()},
+  saveSongBpm:function(){saveRedesignSongBpm()},
+  loadRedesignSong:function(name){
+   loadSong(name);renderRedesignSongs();updateReadouts();sequenceTimelineSignature='';renderRedesignSequenceTimeline();
+  },
+  openSongBpm:function(name){openRedesignSongBpmDialog(name)},
+  deleteRedesignSong:async function(name){await deleteSong(name);renderRedesignSongs();updateReadouts()}
+ });
  function closeRedesignSongBpmDialog(){
-  const dialog=byId('song-bpm-dialog');
-  if(dialog&&dialog.open)dialog.close();
+  redesignSongsUI.closeSongBpmDialog();
   redesignBpmSongName='';
  }
  function openRedesignSongBpmDialog(name){
   if(!songs[name])return;
   redesignBpmSongName=name;
   const stored=Number(songs[name].bpm);
-  const input=byId('song-bpm-dialog-input');
-  const label=byId('song-bpm-dialog-name');
-  if(label)label.textContent=name;
-  if(input)input.value=String(Number.isFinite(stored)?Math.max(40,Math.min(220,Math.round(stored))):Math.round(bpm));
-  const dialog=byId('song-bpm-dialog');
-  if(dialog&&!dialog.open)dialog.showModal();
-  setTimeout(function(){if(input){input.focus();input.select()}},20);
+  const value=Number.isFinite(stored)?Math.max(40,Math.min(220,Math.round(stored))):Math.round(bpm);
+  redesignSongsUI.openSongBpmDialog(name,value);
  }
  function saveRedesignSongBpm(){
   const name=redesignBpmSongName;
@@ -6992,63 +6864,11 @@
   setStatus('BPM de "'+name+'" ajustado para '+value);
  }
  function renderRedesignSongs(){
-  const library=byId('redesign-song-library');
-  if(!library)return;
-  const search=String(byId('redesign-song-search')?.value||'').trim().toLocaleLowerCase('pt-BR');
-  const names=Object.keys(songs).sort(function(a,b){return a.localeCompare(b,'pt-BR')}).filter(function(name){return !search||name.toLocaleLowerCase('pt-BR').includes(search)});
-  library.innerHTML='';
-  if(!names.length){
-   const empty=document.createElement('div');
-   empty.className='redesign-song-card';
-   empty.textContent=search?'Nenhuma música encontrada.':'Nenhuma música salva.';
-   library.appendChild(empty);
-   return;
-  }
-  names.forEach(function(name){
-   const data=songs[name]||{};
-   const card=document.createElement('article');
-   card.className='redesign-song-card'+(name===currentSongName?' active':'');
-
-   const title=document.createElement('strong');
-   title.textContent=name;
-
-   const meta=document.createElement('small');
-   const bpmText=Number.isFinite(Number(data.bpm))?data.bpm+' BPM':'BPM atual';
-   const order=Array.isArray(data.ordemSecoes)?data.ordemSecoes:sequenceOrder;
-   const firstSection=order&&order.length?SEQUENCE_SECTION_LABELS[order[0]]:'A';
-   meta.textContent=bpmText+' · início: '+firstSection;
-
-   const actions=document.createElement('div');
-   actions.className='redesign-song-card-actions';
-
-   const load=document.createElement('button');
-   load.type='button';
-   load.textContent=name===currentSongName?'Selecionada':'Carregar';
-   load.onclick=function(){loadSong(name);renderRedesignSongs();updateReadouts();sequenceTimelineSignature='';renderRedesignSequenceTimeline()};
-
-   const bpmButton=document.createElement('button');
-   bpmButton.type='button';
-   bpmButton.className='redesign-song-bpm';
-   bpmButton.textContent='BPM';
-   bpmButton.title='Ajustar o BPM de '+name;
-   bpmButton.setAttribute('aria-label','Ajustar BPM de '+name);
-   bpmButton.onclick=function(){openRedesignSongBpmDialog(name)};
-
-   const remove=document.createElement('button');
-   remove.type='button';
-   remove.className='redesign-song-delete';
-   remove.textContent='🗑';
-   remove.title='Excluir '+name;
-   remove.setAttribute('aria-label','Excluir '+name);
-   remove.onclick=async function(){
-    await deleteSong(name);
-    renderRedesignSongs();
-    updateReadouts();
-   };
-
-   actions.append(load,bpmButton,remove);
-   card.append(title,meta,actions);
-   library.appendChild(card);
+  redesignSongsUI.renderRedesignSongs({
+   songs:songs,
+   currentSongName:currentSongName,
+   sequenceOrder:sequenceOrder,
+   sectionLabels:SEQUENCE_SECTION_LABELS
   });
  }
 
@@ -7874,23 +7694,7 @@
   byId('redesign-voices-volume').oninput=function(){setVoicesVolume(this.value)};
   byId('redesign-drums-volume').oninput=function(){setDrumsVolume(this.value)};
   byId('redesign-center-keyboard').onclick=function(){centerKeyboardView('smooth')};
-  byId('redesign-song-search').oninput=renderRedesignSongs;
-  byId('song-bpm-dialog-cancel').onclick=closeRedesignSongBpmDialog;
-  byId('song-bpm-dialog-save').onclick=saveRedesignSongBpm;
-  byId('song-bpm-dialog-input').addEventListener('keydown',function(event){
-   if(event.key==='Enter'){
-    event.preventDefault();
-    saveRedesignSongBpm();
-   }
-  });
-  byId('song-bpm-dialog').addEventListener('cancel',function(event){
-   event.preventDefault();
-   closeRedesignSongBpmDialog();
-  });
-  byId('redesign-manage-songs').onclick=function(){byId('songs-open')?.click()};
-  byId('redesign-new-song').onclick=function(){byId('songs-open')?.click();setTimeout(function(){byId('song-new')?.click()},40)};
-  byId('redesign-import-song').onclick=function(){byId('song-import')?.click()};
-  byId('redesign-export-song').onclick=function(){byId('song-export')?.click()};
+  redesignSongsUI.bindRedesignControls();
   byId('instrument')?.addEventListener('change',function(){setTimeout(function(){centerKeyboardView('auto')},30)});
  }
 
--- a/sw.js
+++ b/sw.js
@@ -2,7 +2,7 @@
 
 const CACHE_PREFIX = 'gera-pwa-';
 const LEGACY_CACHE_PREFIX = 'teclado-virtual-pwa-';
-const CACHE_NAME = CACHE_PREFIX + 'v3.15.26';
+const CACHE_NAME = CACHE_PREFIX + 'v3.15.27';
 const OFFLINE_URL = './offline.html';
 
 const PRECACHE_URLS = [
@@ -29,6 +29,7 @@
     "./js/ui/chords-circle.js",
     "./js/ui/drums.js",
     "./js/ui/sequencer.js",
+    "./js/ui/songs-library.js",
     "./js/audio/core.js",
     "./kit-acustico-selecionado/MAPEAMENTO.txt",
     "./kit-acustico-selecionado/cymbals/crash-1.wav",
--- a/manifest.json
+++ b/manifest.json
@@ -39,5 +39,5 @@
             "purpose": "maskable"
         }
     ],
-    "version": "3.15.26"
+    "version": "3.15.27"
 }
--- /dev/null
+++ b/js/ui/songs-library.js
@@ -0,0 +1,250 @@
+/*
+ * GERA — interface de músicas e biblioteca.
+ * Etapa 8G: concentra renderização e ligações sem decidir persistência ou transporte.
+ */
+(function(global){
+ 'use strict';
+
+ function createController(options){
+  const documentRef=options.document||global.document;
+  const element=options.getElement;
+
+  function updateSongNameDisplay(name){
+   const current=element('song-current-name');
+   if(!current)return;
+   current.textContent=name?'🎵 '+name:'Nenhuma música selecionada';
+   current.title=name||'Nenhuma música selecionada';
+  }
+
+  function renderSongListsLibrary(lists,currentSongName){
+   const box=element('song-lists-list');
+   if(!box)return;
+   box.innerHTML='';
+   const ids=Object.keys(lists).sort(function(a,b){return lists[a].name.localeCompare(lists[b].name,'pt-BR')});
+   if(!ids.length){
+    box.innerHTML='<span class="songs-empty">Nenhuma lista criada.</span>';
+    return;
+   }
+   ids.forEach(function(id){
+    const list=lists[id];
+    const row=documentRef.createElement('div');
+    row.className='song-list-library-item';
+    const open=documentRef.createElement('button');
+    open.type='button';
+    open.className='secondary song-list-open';
+    open.innerHTML='<strong></strong><small></small>';
+    open.querySelector('strong').textContent=list.name;
+    open.querySelector('small').textContent=list.songNames.length+(list.songNames.length===1?' música':' músicas');
+    open.onclick=function(){options.openSongList(id)};
+    const use=documentRef.createElement('button');
+    use.type='button';
+    use.className='secondary';
+    use.textContent='Usar';
+    use.title='Selecionar esta lista no modo compacto';
+    use.onclick=function(){options.useSongList(id,list.songNames.indexOf(currentSongName))};
+    row.append(open,use);
+    box.appendChild(row);
+   });
+  }
+
+  function renderSongListEditor(draft,songNames){
+   if(!draft)return;
+   element('song-list-editor-title').textContent='Editar lista';
+   const available=element('song-list-available');
+   const order=element('song-list-order');
+   available.innerHTML='';
+   order.innerHTML='';
+   songNames.slice().sort(function(a,b){return a.localeCompare(b,'pt-BR')}).forEach(function(name){
+    const label=documentRef.createElement('label');
+    label.className='song-list-check';
+    const check=documentRef.createElement('input');
+    check.type='checkbox';
+    check.checked=draft.songNames.indexOf(name)>=0;
+    check.onchange=function(){options.toggleDraftSong(name,this.checked)};
+    const span=documentRef.createElement('span');
+    span.textContent=name;
+    label.append(check,span);
+    available.appendChild(label);
+   });
+   if(!draft.songNames.length){
+    order.innerHTML='<span class="songs-empty">Nenhuma música adicionada.</span>';
+    return;
+   }
+   draft.songNames.forEach(function(name,index){
+    const row=documentRef.createElement('div');
+    row.className='song-list-order-item';
+    const number=documentRef.createElement('span');
+    number.textContent=String(index+1);
+    const title=documentRef.createElement('strong');
+    title.textContent=name;
+    const up=documentRef.createElement('button');
+    up.type='button';up.textContent='↑';up.disabled=index===0;
+    up.onclick=function(){options.moveDraftSong(index,-1)};
+    const down=documentRef.createElement('button');
+    down.type='button';down.textContent='↓';down.disabled=index===draft.songNames.length-1;
+    down.onclick=function(){options.moveDraftSong(index,1)};
+    const remove=documentRef.createElement('button');
+    remove.type='button';remove.className='danger';remove.textContent='×';
+    remove.onclick=function(){options.removeDraftSong(index)};
+    row.append(number,title,up,down,remove);
+    order.appendChild(row);
+   });
+  }
+
+  function renderSongsList(state){
+   const box=element('songs-list');
+   if(!box)return;
+   renderSongListsLibrary(state.songLists,state.currentSongName);
+   const names=Object.keys(state.songs).sort(function(a,b){return a.localeCompare(b,'pt-BR')});
+   if(!names.length){
+    box.innerHTML='<span class="songs-empty">Nenhuma música salva ainda.</span>';
+    return;
+   }
+   box.innerHTML='';
+   names.forEach(function(name){
+    const row=documentRef.createElement('div');
+    row.className='song-item'+(name===state.currentSongName?' song-active':'');
+    const info=documentRef.createElement('div');
+    info.className='song-library-info';
+    const load=documentRef.createElement('button');
+    load.type='button';load.className='song-load secondary';load.textContent=name;load.title='Carregar "'+name+'"';
+    load.onclick=function(){options.loadSong(name)};
+    const memberships=documentRef.createElement('small');
+    const listNames=state.memberships(name);
+    memberships.textContent=listNames.length?listNames.join(' · '):'Fora de listas';
+    info.append(load,memberships);
+    const lists=documentRef.createElement('button');
+    lists.type='button';lists.className='secondary song-list-membership';lists.textContent='Listas';
+    lists.title='Adicionar ou remover esta música das listas';
+    lists.onclick=function(){options.manageSongLists(name)};
+    const remove=documentRef.createElement('button');
+    remove.type='button';remove.className='song-delete danger';remove.textContent='🗑';remove.title='Excluir "'+name+'"';
+    remove.setAttribute('aria-label','Excluir '+name);
+    remove.onclick=function(){options.deleteSong(name)};
+    row.append(info,lists,remove);
+    box.appendChild(row);
+   });
+  }
+
+  function renderRedesignSongs(state){
+   const library=element('redesign-song-library');
+   if(!library)return;
+   const search=String(element('redesign-song-search').value||'').trim().toLocaleLowerCase('pt-BR');
+   const names=Object.keys(state.songs).sort(function(a,b){return a.localeCompare(b,'pt-BR')}).filter(function(name){return !search||name.toLocaleLowerCase('pt-BR').includes(search)});
+   library.innerHTML='';
+   if(!names.length){
+    const empty=documentRef.createElement('div');
+    empty.className='redesign-song-card';
+    empty.textContent=search?'Nenhuma música encontrada.':'Nenhuma música salva.';
+    library.appendChild(empty);
+    return;
+   }
+   names.forEach(function(name){
+    const data=state.songs[name]||{};
+    const card=documentRef.createElement('article');
+    card.className='redesign-song-card'+(name===state.currentSongName?' active':'');
+    const title=documentRef.createElement('strong');
+    title.textContent=name;
+    const meta=documentRef.createElement('small');
+    const bpmText=Number.isFinite(Number(data.bpm))?data.bpm+' BPM':'BPM atual';
+    const order=Array.isArray(data.ordemSecoes)?data.ordemSecoes:state.sequenceOrder;
+    const firstSection=order&&order.length?state.sectionLabels[order[0]]:'A';
+    meta.textContent=bpmText+' · início: '+firstSection;
+    const actions=documentRef.createElement('div');
+    actions.className='redesign-song-card-actions';
+    const load=documentRef.createElement('button');
+    load.type='button';load.textContent=name===state.currentSongName?'Selecionada':'Carregar';
+    load.onclick=function(){options.loadRedesignSong(name)};
+    const bpmButton=documentRef.createElement('button');
+    bpmButton.type='button';bpmButton.className='redesign-song-bpm';bpmButton.textContent='BPM';bpmButton.title='Ajustar o BPM de '+name;
+    bpmButton.setAttribute('aria-label','Ajustar BPM de '+name);
+    bpmButton.onclick=function(){options.openSongBpm(name)};
+    const remove=documentRef.createElement('button');
+    remove.type='button';remove.className='redesign-song-delete';remove.textContent='🗑';remove.title='Excluir '+name;
+    remove.setAttribute('aria-label','Excluir '+name);
+    remove.onclick=function(){options.deleteRedesignSong(name)};
+    actions.append(load,bpmButton,remove);
+    card.append(title,meta,actions);
+    library.appendChild(card);
+   });
+  }
+
+  function openSongsDialog(name,bpm){
+   options.renderSongs();
+   element('song-name-input').value=name||'';
+   element('song-bpm-input').value=bpm;
+   element('songs-dialog').showModal();
+  }
+  function openSongListDialog(name){
+   element('song-list-editor-name').value=name;
+   const dialog=element('song-list-editor-dialog');
+   if(dialog&&!dialog.open)dialog.showModal();
+  }
+  function closeSongListDialog(){
+   const dialog=element('song-list-editor-dialog');
+   if(dialog&&dialog.open)dialog.close();
+  }
+  function openSongBpmDialog(name,value){
+   element('song-bpm-dialog-name').textContent=name;
+   const input=element('song-bpm-dialog-input');
+   input.value=String(value);
+   const dialog=element('song-bpm-dialog');
+   if(dialog&&!dialog.open)dialog.showModal();
+   setTimeout(function(){input.focus();input.select()},20);
+  }
+  function closeSongBpmDialog(){
+   const dialog=element('song-bpm-dialog');
+   if(dialog&&dialog.open)dialog.close();
+  }
+
+  function bindMainControls(){
+   element('song-export').onclick=function(){options.exportSong()};
+   element('song-import').onclick=function(){options.chooseImport()};
+   element('song-import-file').onchange=function(event){options.importFile(event)};
+   element('songs-open').onclick=function(){options.openSongs()};
+   element('songs-close').onclick=function(){element('songs-dialog').close()};
+   element('song-save').onclick=function(){options.saveSong()};
+   element('song-new').onclick=function(){options.newSong()};
+   element('song-list-create').onclick=function(){
+    options.createSongList(element('song-list-new-name').value);
+    element('song-list-new-name').value='';
+   };
+   element('song-list-editor-close').onclick=function(){options.closeSongList()};
+   element('song-list-editor-cancel').onclick=function(){options.closeSongList()};
+   element('song-list-editor-save').onclick=function(){options.saveSongList()};
+   element('song-list-delete').onclick=function(){options.deleteSongList()};
+   element('song-list-editor-dialog').addEventListener('cancel',function(event){event.preventDefault();options.closeSongList()});
+  }
+
+  function bindRedesignControls(){
+   element('redesign-song-search').oninput=function(){options.renderRedesignSongs()};
+   element('song-bpm-dialog-cancel').onclick=function(){options.closeSongBpm()};
+   element('song-bpm-dialog-save').onclick=function(){options.saveSongBpm()};
+   element('song-bpm-dialog-input').addEventListener('keydown',function(event){
+    if(event.key==='Enter'){event.preventDefault();options.saveSongBpm()}
+   });
+   element('song-bpm-dialog').addEventListener('cancel',function(event){event.preventDefault();options.closeSongBpm()});
+   element('redesign-manage-songs').onclick=function(){element('songs-open').click()};
+   element('redesign-new-song').onclick=function(){element('songs-open').click();setTimeout(function(){element('song-new').click()},40)};
+   element('redesign-import-song').onclick=function(){element('song-import').click()};
+   element('redesign-export-song').onclick=function(){element('song-export').click()};
+  }
+
+  return Object.freeze({
+   updateSongNameDisplay:updateSongNameDisplay,
+   renderSongListsLibrary:renderSongListsLibrary,
+   renderSongListEditor:renderSongListEditor,
+   renderSongsList:renderSongsList,
+   renderRedesignSongs:renderRedesignSongs,
+   openSongsDialog:openSongsDialog,
+   openSongListDialog:openSongListDialog,
+   closeSongListDialog:closeSongListDialog,
+   openSongBpmDialog:openSongBpmDialog,
+   closeSongBpmDialog:closeSongBpmDialog,
+   bindMainControls:bindMainControls,
+   bindRedesignControls:bindRedesignControls
+  });
+ }
+
+ global.GeraSongsLibraryUI=Object.freeze({createController:createController});
+})(typeof window!=='undefined'?window:globalThis);
