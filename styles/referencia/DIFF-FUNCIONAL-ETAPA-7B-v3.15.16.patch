--- a/index.html
+++ b/index.html
@@ -15,7 +15,7 @@
 <link rel="icon" type="image/png" sizes="512x512" href="./icons/icon-512.png">
 <link rel="apple-touch-icon" href="./icons/icon-192.png">
 <meta name="screen-orientation" content="landscape">
-<title>GERA v3.15.15</title>
+<title>GERA v3.15.16</title>
 <script src="./js/storage.js"></script>
@@ -38,7 +38,7 @@
 <header class="app-header">
   <div class="header-brand">
-    <h1><span class="gera-brand-mark" aria-hidden="true"><img src="icons/icon-192.png" alt=""></span><span class="gera-title">GERA</span><span class="gera-subtitle">Gerador de Acompanhamentos</span><small class="app-version">v3.15.15</small></h1>
+    <h1><span class="gera-brand-mark" aria-hidden="true"><img src="icons/icon-192.png" alt=""></span><span class="gera-title">GERA</span><span class="gera-subtitle">Gerador de Acompanhamentos</span><small class="app-version">v3.15.16</small></h1>
@@ -163,7 +163,7 @@
     <span class="redesign-logo">GERA</span>
     <span class="redesign-tagline">Gerador de Acompanhamentos</span>
-    <small>v3.15.15</small>
+    <small>v3.15.16</small>
@@ -5771,7 +5771,7 @@ function bindDrums(){
- $('drum-engine').onchange=e=>{drumEngine=e.target.value;localStorage.setItem('tecladoVirtualDrumEngine',drumEngine);refreshDrumEngineStatus();if(drumEngine==='acoustic')requestAcousticDrums();};
+ $('drum-engine').onchange=e=>{drumEngine=e.target.value;GeraStorage.musicalSettings.setDrumEngine(drumEngine);refreshDrumEngineStatus();if(drumEngine==='acoustic')requestAcousticDrums();};
@@ -6911,7 +6911,7 @@ function applySettings(s){
- if(sustainMode!=='pressed'&&sustainMode!=='hold'&&sustainMode!=='next')sustainMode='pressed';releaseMs=+s.releaseMs||650;attackMs=+s.attackMs||20;chordMode=s.chordMode||'together';arpInterval=+s.arpInterval||280;arpPattern=s.arpPattern||'up';inversion=+s.inversion||0;bassEnabled=!!s.bassEnabled;latchEnabled=!!s.latchEnabled;splitEnabled=!!s.splitEnabled;splitInstrument=normalizeInstrumentId(s.splitInstrument||'organ');velocityEnabled=s.velocityEnabled!==false;glissEnabled=s.glissEnabled!==false;bpm=+s.bpm||100;rhythmPattern=s.rhythmPattern||'whole';pendingRhythmPattern=null;chordChangeMode=s.chordChangeMode||'nextBar';drumEngine=s.drumEngine||localStorage.getItem('tecladoVirtualDrumEngine')||'acoustic';const v=s.values||FACTORY_SETTINGS.values;$('master-volume').value=(v.master == null ? 68 : v.master);$('drum-volume').value=(v.drum == null ? 70 : v.drum);$('keyboard-volume').value=(v.keyboard == null ? 92 : v.keyboard);$('chord-volume').value=(v.chord == null ? 72 : v.chord);$('bass-volume').value=(v.bass == null ? 56 : v.bass);syncSettingsUI();
+ if(sustainMode!=='pressed'&&sustainMode!=='hold'&&sustainMode!=='next')sustainMode='pressed';releaseMs=+s.releaseMs||650;attackMs=+s.attackMs||20;chordMode=s.chordMode||'together';arpInterval=+s.arpInterval||280;arpPattern=s.arpPattern||'up';inversion=+s.inversion||0;bassEnabled=!!s.bassEnabled;latchEnabled=!!s.latchEnabled;splitEnabled=!!s.splitEnabled;splitInstrument=normalizeInstrumentId(s.splitInstrument||'organ');velocityEnabled=s.velocityEnabled!==false;glissEnabled=s.glissEnabled!==false;bpm=+s.bpm||100;rhythmPattern=s.rhythmPattern||'whole';pendingRhythmPattern=null;chordChangeMode=s.chordChangeMode||'nextBar';drumEngine=s.drumEngine||GeraStorage.musicalSettings.getDrumEngine()||'acoustic';const v=s.values||FACTORY_SETTINGS.values;$('master-volume').value=(v.master == null ? 68 : v.master);$('drum-volume').value=(v.drum == null ? 70 : v.drum);$('keyboard-volume').value=(v.keyboard == null ? 92 : v.keyboard);$('chord-volume').value=(v.chord == null ? 72 : v.chord);$('bass-volume').value=(v.bass == null ? 56 : v.bass);syncSettingsUI();
@@ -7089,7 +7089,7 @@
-drumEngine=localStorage.getItem('tecladoVirtualDrumEngine')||'acoustic';
+drumEngine=GeraStorage.musicalSettings.getDrumEngine()||'acoustic';
 renderKeyboard();
--- a/js/storage.js
+++ b/js/storage.js
@@ -1,8 +1,9 @@
 /*
  * Acesso centralizado ao localStorage do GERA.
  *
- * Etapa 7A: somente preferências visuais e tema. As chaves e os valores
- * persistidos continuam idênticos aos da versão legada.
+ * Etapas 7A e 7B: preferências visuais, tema e configurações musicais
+ * globais simples. As chaves e os valores persistidos continuam idênticos
+ * aos da versão legada.
@@ -11,7 +12,8 @@
   redesignTab:'geraRedesignTab',
   globalMutePosition:'geraGlobalMutePositionV1',
-  redesignRailCollapsed:'geraRedesignRailCollapsed'
+  redesignRailCollapsed:'geraRedesignRailCollapsed',
+  drumEngine:'tecladoVirtualDrumEngine'
  });
@@ -44,8 +46,14 @@
   setRedesignRailCollapsed:function(value){return write(keys.redesignRailCollapsed,value)}
  });
+ const musicalSettings=Object.freeze({
+  getDrumEngine:function(){return read(keys.drumEngine,null)},
+  setDrumEngine:function(value){return write(keys.drumEngine,value)}
+ });
  global.GeraStorage=Object.freeze({
   keys:keys,
-  preferences:preferences
+  preferences:preferences,
+  musicalSettings:musicalSettings
  });
--- a/sw.js
+++ b/sw.js
@@ -2,7 +2,7 @@
 const CACHE_PREFIX = 'gera-pwa-';
 const LEGACY_CACHE_PREFIX = 'teclado-virtual-pwa-';
-const CACHE_NAME = CACHE_PREFIX + 'v3.15.15';
+const CACHE_NAME = CACHE_PREFIX + 'v3.15.16';
 const OFFLINE_URL = './offline.html';
--- a/manifest.json
+++ b/manifest.json
@@ -39,5 +39,5 @@
-    "version": "3.15.15"
+    "version": "3.15.16"
 }
