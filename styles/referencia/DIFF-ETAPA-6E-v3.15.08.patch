--- GERA-PWA-v3.15.07-etapa-6D-fronteiras-compasso/index.html
+++ GERA-PWA-v3.15.08-etapa-6E-mudancas-bpm/index.html
@@ -15,7 +15,7 @@
 <link rel="icon" type="image/png" sizes="512x512" href="./icons/icon-512.png">
 <link rel="apple-touch-icon" href="./icons/icon-192.png">
 <meta name="screen-orientation" content="landscape">
-<title>GERA v3.15.07</title>
+<title>GERA v3.15.08</title>
 <script>
 (function(){
  try{
@@ -37,7 +37,7 @@
 <div id="rotate-msg"><div class="icon">📱</div><h2>Gire o dispositivo</h2><p>O GERA foi organizado para uso horizontal.</p></div>
 <header class="app-header">
   <div class="header-brand">
-    <h1><span class="gera-brand-mark" aria-hidden="true"><img src="icons/icon-192.png" alt=""></span><span class="gera-title">GERA</span><span class="gera-subtitle">Gerador de Acompanhamentos</span><small class="app-version">v3.15.07</small></h1>
+    <h1><span class="gera-brand-mark" aria-hidden="true"><img src="icons/icon-192.png" alt=""></span><span class="gera-title">GERA</span><span class="gera-subtitle">Gerador de Acompanhamentos</span><small class="app-version">v3.15.08</small></h1>
   </div>
   <div class="header-actions">
     <button type="button" id="theme-cycle" class="secondary compact-theme-button" aria-label="Alterar tema do aplicativo" title="Alterar tema">🎨 Escuro</button>
@@ -162,7 +162,7 @@
   <div class="redesign-brandmark">
     <span class="redesign-logo">GERA</span>
     <span class="redesign-tagline">Gerador de Acompanhamentos</span>
-    <small>v3.15.07</small>
+    <small>v3.15.08</small>
   </div>
   <button class="redesign-song-pill" id="redesign-song-pill" type="button" title="Escolher música">♫ <span id="redesign-song-name">Nenhuma música</span></button>
   <div id="redesign-header-text-preview" class="sequence-text-preview sequence-text-preview-floating" aria-live="polite">
@@ -807,6 +807,7 @@
 <script src="./js/transport/clock.js"></script>
 <script src="./js/transport/scheduler.js"></script>
 <script src="./js/transport/boundaries.js"></script>
+<script src="./js/transport/tempo.js"></script>
 <script src="./js/audio/core.js"></script>
 <script>
 "use strict";
@@ -1642,23 +1643,9 @@
  }
 }
 function requestBpmChange(value){
- const requested=normalizedBpm(value);
+ const requested=transportTempoController.requestChange(value,transportRunning);
  const input=$('bpm');
  if(input)input.value=String(requested);
-
- if(transportRunning){
-  if(requested===bpm){
-   pendingBpm=null;
-  }else{
-   pendingBpm=requested;
-  }
-  updateBpmDisplay();
-  return;
- }
-
- bpm=requested;
- transportTempoBpm=bpm;
- pendingBpm=null;
  updateBpmDisplay();
 }
 function syncMetronomeButton(){
@@ -1714,9 +1701,7 @@
  else startMetronome();
 }
 function applyPendingBpmAtBoundary(value){
- bpm=normalizedBpm(value);
- transportTempoBpm=bpm;
- pendingBpm=null;
+ transportTempoController.applyAtBoundary(value);
  const input=$('bpm');
  if(input)input.value=String(bpm);
  updateBpmDisplay();
@@ -1724,6 +1709,12 @@
   $('drum-status').textContent=drumPattern.toUpperCase()+' · '+bpm+' BPM';
  }
 }
+const transportTempoController=GeraTransportTempo.createController({
+ normalize:normalizedBpm,
+ currentTime:function(){return audioCtx.currentTime},
+ onBoundaryApply:applyPendingBpmAtBoundary,
+ trackEvent:function(tempoEvent){transportEvents.push(tempoEvent)}
+});
 function stopMasterTransport(){
  transportRunning=false;
  clearTimeout(transportTimer);
@@ -1738,15 +1729,14 @@
  drumCompletedAction='';
  drumActionMuteFrom=0;drumActionMuteUntil=0;
  sequenceEndActionInProgress='';
- if(pendingBpm!==null)applyPendingBpmAtBoundary(pendingBpm);
- else transportTempoBpm=bpm;
+ transportTempoController.settleOnStop();
  syncDrumActionButtons();
 }
 function maybeStopMasterTransport(){if(!transportNeeded())stopMasterTransport()}
 function ensureMasterTransport(){
  ensureAudio();
  if(transportRunning)return;
- transportRunning=true;transportStep=0;transportBar=0;transportTempoBpm=bpm;pendingBpm=null;transportNextTime=audioCtx.currentTime+.08;clearTimeout(transportTimer);clearTransportEvents();transportScheduler();
+ transportRunning=true;transportStep=0;transportBar=0;transportTempoController.resetForStart();transportNextTime=audioCtx.currentTime+.08;clearTimeout(transportTimer);clearTransportEvents();transportScheduler();
 }
 function stopAccompaniment(){sequenceChordOctaveOverride=null;clearAccompanimentSchedule();latchedRoot=null;latchedType=null;latchedButton=null;pendingRoot=null;pendingType=null;pendingButton=null;document.querySelectorAll('.chord.latched,.chord.queued').forEach(b=>b.classList.remove('latched','queued'));releaseAll(180);syncInstrumentChangeLock();maybeStopMasterTransport()}
 function markLatched(button){document.querySelectorAll('.chord.latched').forEach(b=>b.classList.remove('latched'));button?.classList.add('latched')}
@@ -5681,16 +5671,7 @@
  executeBarSegment(latchedRoot,latchedType,latchedButton,segmentStart,Math.min(duration,segmentStart+boundaryDuration));
 }
 function handleTransportSchedulerPulse(step,when){
-   if(step===0&&pendingBpm!==null){
-    const nextBpm=normalizedBpm(pendingBpm);
-    pendingBpm=null;
-    transportTempoBpm=nextBpm;
-    const tempoDelay=Math.max(0,(when-audioCtx.currentTime)*1000);
-    const tempoEvent=setTimeout(function(){
-     applyPendingBpmAtBoundary(nextBpm);
-    },tempoDelay);
-    transportEvents.push(tempoEvent);
-   }
+   transportTempoController.schedulePendingAtStep(step,when);
 
    const stepDur=GeraTransportClock.stepDurationSeconds(transportTempoBpm);
    if(step===0&&accompanimentStopQueued){
--- /dev/null
+++ GERA-PWA-v3.15.08-etapa-6E-mudancas-bpm/js/transport/tempo.js
@@ -0,0 +1,79 @@
+/*
+ * Aplicação das mudanças de BPM no transporte mestre do GERA.
+ *
+ * Este módulo modifica exclusivamente o estado canônico mantido em
+ * GeraState.tempo. A interface, a persistência, o scheduler, as fronteiras,
+ * a bateria e a sequência permanecem no núcleo legado.
+ */
+'use strict';
+
+(function(global){
+ function createController(options){
+  const tempo=global.GeraState.tempo;
+
+  function requestChange(value,isTransportRunning){
+   const requested=options.normalize(value);
+   if(isTransportRunning){
+    if(requested===tempo.bpm){
+     tempo.pendingBpm=null;
+    }else{
+     tempo.pendingBpm=requested;
+    }
+    return requested;
+   }
+
+   tempo.bpm=requested;
+   tempo.transportTempoBpm=tempo.bpm;
+   tempo.pendingBpm=null;
+   return requested;
+  }
+
+  function applyAtBoundary(value){
+   tempo.bpm=options.normalize(value);
+   tempo.transportTempoBpm=tempo.bpm;
+   tempo.pendingBpm=null;
+   return tempo.bpm;
+  }
+
+  function schedulePendingAtStep(step,when){
+   if(step!==0||tempo.pendingBpm===null)return null;
+   const nextBpm=options.normalize(tempo.pendingBpm);
+   tempo.pendingBpm=null;
+   tempo.transportTempoBpm=nextBpm;
+   const tempoDelay=Math.max(0,(when-options.currentTime())*1000);
+   const tempoEvent=setTimeout(function(){
+    options.onBoundaryApply(nextBpm);
+   },tempoDelay);
+   options.trackEvent(tempoEvent);
+   return nextBpm;
+  }
+
+  function settleOnStop(){
+   if(tempo.pendingBpm!==null){
+    options.onBoundaryApply(tempo.pendingBpm);
+   }else{
+    tempo.transportTempoBpm=tempo.bpm;
+   }
+  }
+
+  function resetForStart(){
+   tempo.transportTempoBpm=tempo.bpm;
+   tempo.pendingBpm=null;
+  }
+
+  return Object.freeze({
+   requestChange:requestChange,
+   applyAtBoundary:applyAtBoundary,
+   schedulePendingAtStep:schedulePendingAtStep,
+   settleOnStop:settleOnStop,
+   resetForStart:resetForStart
+  });
+ }
+
+ Object.defineProperty(global,'GeraTransportTempo',{
+  configurable:false,
+  enumerable:true,
+  writable:false,
+  value:Object.freeze({createController:createController})
+ });
+})(typeof window!=='undefined'?window:globalThis);
--- GERA-PWA-v3.15.07-etapa-6D-fronteiras-compasso/sw.js
+++ GERA-PWA-v3.15.08-etapa-6E-mudancas-bpm/sw.js
@@ -2,7 +2,7 @@
 
 const CACHE_PREFIX = 'gera-pwa-';
 const LEGACY_CACHE_PREFIX = 'teclado-virtual-pwa-';
-const CACHE_NAME = CACHE_PREFIX + 'v3.15.07';
+const CACHE_NAME = CACHE_PREFIX + 'v3.15.08';
 const OFFLINE_URL = './offline.html';
 
 const PRECACHE_URLS = [
@@ -16,6 +16,7 @@
     "./js/transport/clock.js",
     "./js/transport/scheduler.js",
     "./js/transport/boundaries.js",
+    "./js/transport/tempo.js",
     "./js/audio/core.js",
     "./kit-acustico-selecionado/MAPEAMENTO.txt",
     "./kit-acustico-selecionado/cymbals/crash-1.wav",
--- GERA-PWA-v3.15.07-etapa-6D-fronteiras-compasso/manifest.json
+++ GERA-PWA-v3.15.08-etapa-6E-mudancas-bpm/manifest.json
@@ -39,5 +39,5 @@
             "purpose": "maskable"
         }
     ],
-    "version": "3.15.07"
+    "version": "3.15.08"
 }
