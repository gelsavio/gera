diff --git a/index.html b/../GERA-PWA-v3.15.13-etapa-6J-painel-compacto-contadores/index.html
index 24e864e..f5e867a 100644
--- a/index.html
+++ b/../GERA-PWA-v3.15.13-etapa-6J-painel-compacto-contadores/index.html
@@ -15,7 +15,7 @@
 <link rel="icon" type="image/png" sizes="512x512" href="./icons/icon-512.png">
 <link rel="apple-touch-icon" href="./icons/icon-192.png">
 <meta name="screen-orientation" content="landscape">
-<title>GERA v3.15.12</title>
+<title>GERA v3.15.13</title>
 <script>
 (function(){
  try{
@@ -37,7 +37,7 @@
 <div id="rotate-msg"><div class="icon">📱</div><h2>Gire o dispositivo</h2><p>O GERA foi organizado para uso horizontal.</p></div>
 <header class="app-header">
   <div class="header-brand">
-    <h1><span class="gera-brand-mark" aria-hidden="true"><img src="icons/icon-192.png" alt=""></span><span class="gera-title">GERA</span><span class="gera-subtitle">Gerador de Acompanhamentos</span><small class="app-version">v3.15.12</small></h1>
+    <h1><span class="gera-brand-mark" aria-hidden="true"><img src="icons/icon-192.png" alt=""></span><span class="gera-title">GERA</span><span class="gera-subtitle">Gerador de Acompanhamentos</span><small class="app-version">v3.15.13</small></h1>
   </div>
   <div class="header-actions">
     <button type="button" id="theme-cycle" class="secondary compact-theme-button" aria-label="Alterar tema do aplicativo" title="Alterar tema">🎨 Escuro</button>
@@ -162,7 +162,7 @@
   <div class="redesign-brandmark">
     <span class="redesign-logo">GERA</span>
     <span class="redesign-tagline">Gerador de Acompanhamentos</span>
-    <small>v3.15.12</small>
+    <small>v3.15.13</small>
   </div>
   <button class="redesign-song-pill" id="redesign-song-pill" type="button" title="Escolher música">♫ <span id="redesign-song-name">Nenhuma música</span></button>
   <div id="redesign-header-text-preview" class="sequence-text-preview sequence-text-preview-floating" aria-live="polite">
@@ -812,6 +812,7 @@
 <script src="./js/transport/chord-sequence-sync.js"></script>
 <script src="./js/transport/sequence-transitions.js"></script>
 <script src="./js/transport/coordinator.js"></script>
+<script src="./js/ui/transport-status.js"></script>
 <script src="./js/audio/core.js"></script>
 <script>
 "use strict";
@@ -6385,49 +6386,48 @@ function compactUpcomingTextCues(elapsed,count){
  return result;
 }
 
+const transportStatusConsumer=GeraTransportStatus.createConsumer({
+ getElement:function(id){return $(id)},
+ getSnapshot:function(){
+  const status=$('status');
+  const keyboardVolume=$('keyboard-volume');
+  const chordVolume=$('chord-volume');
+  const bassVolume=$('bass-volume');
+  const drumVolume=$('drum-volume');
+  return Object.freeze({
+   capoSemitones:capoSemitones,
+   octave:octave,
+   sequenceHoldLoop:sequenceHoldLoop,
+   sequencePlaying:sequencePlaying,
+   sequenceStartQueued:sequenceStartQueued,
+   drumRunning:drumRunning,
+   drumStartQueued:drumStartQueued,
+   currentSongName:currentSongName,
+   activeSequenceSection:activeSequenceSection,
+   queuedSequenceSection:queuedSequenceSection,
+   sequencePendingTransition:sequencePendingTransition,
+   sectionLabels:SEQUENCE_SECTION_LABELS,
+   bpm:bpm,
+   sequenceAuto:sequenceAuto,
+   sequenceAutoEnd:sequenceAutoEnd,
+   sequenceSections:sequenceSections,
+   sequenceIndex:sequenceIndex,
+   sequenceEighthUnitsRemaining:sequenceEighthUnitsRemaining,
+   currentSectionRepetition:currentSectionRepetition,
+   sectionRepeatValue:sectionRepeatValue,
+   barDuration:barDuration,
+   keyboardVolume:Number(keyboardVolume?keyboardVolume.value:100),
+   chordVolume:Number(chordVolume?chordVolume.value:78),
+   bassVolume:Number(bassVolume?bassVolume.value:62),
+   drumVolume:Number(drumVolume?drumVolume.value:70),
+   statusText:status?status.textContent:''
+  });
+ },
+ syncListControls:syncCompactListControls,
+ renderCarousel:renderCompactSequenceCarousel
+});
 function syncCompactControls(){
- const capo=$('compact-capo-value');
- const oct=$('compact-octave-value');
- const loop=$('compact-loop');
- const play=$('compact-play');
- const sequenceOnly=$('compact-sequence-only');
- const drum=$('compact-drum-only');
- const current=$('compact-current-song');
- const selectedSong=$('compact-selected-song');
-
- if(capo)capo.textContent=capoSemitones>0?'+'+capoSemitones:String(capoSemitones);
- if(oct)oct.textContent=octave>0?'+'+octave:String(octave);
-
- if(loop){
-  loop.classList.toggle('active',sequenceHoldLoop);
-  loop.textContent=sequenceHoldLoop?'🔁 Sequência em loop':'🔁 Sequência atual';
- }
- const musicActive=sequencePlaying||sequenceStartQueued;
- const drumsActive=drumRunning||drumStartQueued;
- const anyTransportActive=musicActive||drumsActive;
- if(play){
-  play.classList.toggle('active',anyTransportActive);
-  play.setAttribute('aria-pressed',String(anyTransportActive));
-  play.textContent=anyTransportActive?'■ Parar Música + Bateria':'▶ Música + Bateria';
- }
- if(sequenceOnly){
-  sequenceOnly.classList.toggle('active',musicActive);
-  sequenceOnly.setAttribute('aria-pressed',String(musicActive));
-  sequenceOnly.textContent=musicActive?'■ Parar Música':'🎼 Só Música';
- }
- if(drum){
-  drum.classList.toggle('active',drumsActive);
-  drum.setAttribute('aria-pressed',String(drumsActive));
-  drum.textContent=drumsActive?'■ Parar Bateria':'🥁 Só Bateria';
- }
- if(selectedSong)selectedSong.textContent=currentSongName||'Nenhuma música selecionada';
- if(current){
-  current.textContent=currentSongName
-   ?SEQUENCE_SECTION_LABELS[activeSequenceSection]+' · '+bpm+' BPM'
-   :'Nenhuma música carregada';
- }
- syncCompactListControls();
- renderCompactSequenceCarousel();
+ transportStatusConsumer.syncCompactControls();
 }
 function setCompactMode(active){
  compactModeActive=active===true;
@@ -7293,45 +7293,7 @@ $('song-new').onclick=async ()=>{
   });
  }
  function updateReadouts(){
-  const song=String(currentSongName||'Nenhuma música');
-  const songName=byId('redesign-song-name');
-  const bpmTop=byId('redesign-bpm-readout');
-  const bpmBottom=byId('redesign-mixer-bpm');
-  const play=byId('redesign-play');
-  const playLoop=byId('redesign-play-loop');
-  const currentLoop=byId('redesign-current-loop');
-  const statusSource=byId('status');
-  const statusTarget=byId('redesign-status');
-  const voices=byId('redesign-voices-volume');
-  const drums=byId('redesign-drums-volume');
-  const keyboardVolume=Number(byId('keyboard-volume')?.value||100);
-  const chordVolume=Number(byId('chord-volume')?.value||78);
-  const bassVolume=Number(byId('bass-volume')?.value||62);
-  const drumVolume=Number(byId('drum-volume')?.value||70);
-  if(songName)songName.textContent=song;
-  if(bpmTop)bpmTop.textContent=String(bpm);
-  if(bpmBottom)bpmBottom.textContent=String(bpm);
-  if(play){
-   play.textContent='▶';
-   play.classList.toggle('active',sequencePlaying&&sequenceAutoEnd&&!sequenceAuto);
-   play.setAttribute('aria-pressed',String(sequencePlaying&&sequenceAutoEnd&&!sequenceAuto));
-  }
-  if(playLoop){
-   playLoop.textContent='↻';
-   playLoop.classList.toggle('active',sequencePlaying&&sequenceAuto&&!sequenceAutoEnd);
-   playLoop.setAttribute('aria-pressed',String(sequencePlaying&&sequenceAuto&&!sequenceAutoEnd));
-  }
-  if(currentLoop){
-   currentLoop.textContent='🔁';
-   currentLoop.classList.toggle('active',sequenceHoldLoop);
-   currentLoop.setAttribute('aria-pressed',String(sequenceHoldLoop));
-   currentLoop.title=sequenceHoldLoop
-    ?'Liberar a sequência atual e continuar a progressão'
-    :'Manter somente a sequência atual em loop';
-  }
-  if(voices)voices.value=String(Math.round((keyboardVolume+chordVolume+bassVolume)/3));
-  if(drums)drums.value=String(drumVolume);
-  if(statusSource&&statusTarget)statusTarget.textContent=statusSource.textContent;
+  transportStatusConsumer.updateReadouts();
  }
 
  let songSignature='';
@@ -7442,45 +7404,18 @@ $('song-new').onclick=async ()=>{
  }
 
  let redesignLastScrolledSection='';
- function sequenceItemUnits(item){
-  const fraction=Number(item&&item.fraction);
-  if(!Number.isFinite(fraction)||fraction<=0)return 8;
-  return Math.max(1,Math.round(fraction*8));
- }
+ function sequenceItemUnits(item){return transportStatusConsumer.sequenceItemUnits(item)}
  function sequenceTotalUnits(section){
-  const items=Array.isArray(sequenceSections[section])?sequenceSections[section]:[];
-  return items.reduce(function(total,item){return total+sequenceItemUnits(item)},0);
+  return transportStatusConsumer.sequenceTotalUnits(section);
  }
  function sequenceRemainingUnits(section){
-  const items=Array.isArray(sequenceSections[section])?sequenceSections[section]:[];
-  const total=sequenceTotalUnits(section);
-  if(!sequencePlaying||section!==activeSequenceSection)return total;
-  if(sequenceIndex<0)return total;
-
-  let current=Math.max(0,Number(sequenceEighthUnitsRemaining)||0);
-  for(let index=sequenceIndex+1;index<items.length;index++)current+=sequenceItemUnits(items[index]);
-
-  if(sequenceHoldLoop)return current;
-  const configured=Math.max(1,sectionRepeatValue(section));
-  const remainingPasses=Math.max(0,configured-currentSectionRepetition);
-  return current+(remainingPasses*total);
+  return transportStatusConsumer.sequenceRemainingUnits(section);
  }
  function formatSequenceCountdown(units){
-  const seconds=Math.max(0,Math.ceil((units*barDuration())/8000));
-  const minutes=Math.floor(seconds/60);
-  const remainder=seconds%60;
-  return String(minutes).padStart(2,'0')+':'+String(remainder).padStart(2,'0');
+  return transportStatusConsumer.formatSequenceCountdown(units);
  }
  function syncCompactSequenceCountdown(){
-  const countdown=byId('compact-seq-countdown');
-  const label=byId('compact-seq-countdown-label');
-  const value=byId('compact-seq-countdown-value');
-  if(!countdown||!label||!value)return;
-  const active=sequencePlaying||sequenceStartQueued;
-  countdown.hidden=!active;
-  if(!active)return;
-  label.textContent='Restante · '+(SEQUENCE_SECTION_LABELS[activeSequenceSection]||('Sequência '+activeSequenceSection));
-  value.textContent=formatSequenceCountdown(sequenceRemainingUnits(activeSequenceSection));
+  transportStatusConsumer.syncCompactSequenceCountdown();
  }
  function keepPlayingSequenceCardVisible(card,section){
   if(!card||!sequencePlaying||section!==activeSequenceSection)return;
diff --git a/manifest.json b/../GERA-PWA-v3.15.13-etapa-6J-painel-compacto-contadores/manifest.json
index ab46b48..6724389 100644
--- a/manifest.json
+++ b/../GERA-PWA-v3.15.13-etapa-6J-painel-compacto-contadores/manifest.json
@@ -39,5 +39,5 @@
             "purpose": "maskable"
         }
     ],
-    "version": "3.15.12"
+    "version": "3.15.13"
 }
diff --git a/sw.js b/../GERA-PWA-v3.15.13-etapa-6J-painel-compacto-contadores/sw.js
index 2119656..fc5ddcd 100644
--- a/sw.js
+++ b/../GERA-PWA-v3.15.13-etapa-6J-painel-compacto-contadores/sw.js
@@ -2,7 +2,7 @@
 
 const CACHE_PREFIX = 'gera-pwa-';
 const LEGACY_CACHE_PREFIX = 'teclado-virtual-pwa-';
-const CACHE_NAME = CACHE_PREFIX + 'v3.15.12';
+const CACHE_NAME = CACHE_PREFIX + 'v3.15.13';
 const OFFLINE_URL = './offline.html';
 
 const PRECACHE_URLS = [
@@ -21,6 +21,7 @@ const PRECACHE_URLS = [
     "./js/transport/chord-sequence-sync.js",
     "./js/transport/sequence-transitions.js",
     "./js/transport/coordinator.js",
+    "./js/ui/transport-status.js",
     "./js/audio/core.js",
     "./kit-acustico-selecionado/MAPEAMENTO.txt",
     "./kit-acustico-selecionado/cymbals/crash-1.wav",
diff --git a/../GERA-PWA-v3.15.13-etapa-6J-painel-compacto-contadores/js/ui/transport-status.js b/../GERA-PWA-v3.15.13-etapa-6J-painel-compacto-contadores/js/ui/transport-status.js
new file mode 100644
index 0000000..fe3753b
--- /dev/null
+++ b/../GERA-PWA-v3.15.13-etapa-6J-painel-compacto-contadores/js/ui/transport-status.js
@@ -0,0 +1,151 @@
+/**
+ * Consumidor visual do estado do transporte do GERA.
+ * Não mantém estado musical, não agenda pulsos e não recria elementos do DOM.
+ */
+(function(global){
+ 'use strict';
+
+ function createConsumer(options){
+  const getElement=options.getElement;
+  const getSnapshot=options.getSnapshot;
+  const syncListControls=options.syncListControls||function(){};
+  const renderCarousel=options.renderCarousel||function(){};
+
+  function element(id){return getElement(id)}
+  function snapshot(){return getSnapshot()}
+
+  function sequenceItemUnits(item){
+   const fraction=Number(item&&item.fraction);
+   if(!Number.isFinite(fraction)||fraction<=0)return 8;
+   return Math.max(1,Math.round(fraction*8));
+  }
+  function sequenceTotalUnits(section,state){
+   const current=state||snapshot();
+   const items=Array.isArray(current.sequenceSections[section])?current.sequenceSections[section]:[];
+   return items.reduce(function(total,item){return total+sequenceItemUnits(item)},0);
+  }
+  function sequenceRemainingUnits(section,state){
+   const current=state||snapshot();
+   const items=Array.isArray(current.sequenceSections[section])?current.sequenceSections[section]:[];
+   const total=sequenceTotalUnits(section,current);
+   if(!current.sequencePlaying||section!==current.activeSequenceSection)return total;
+   if(current.sequenceIndex<0)return total;
+   let remaining=Math.max(0,Number(current.sequenceEighthUnitsRemaining)||0);
+   for(let index=current.sequenceIndex+1;index<items.length;index++)remaining+=sequenceItemUnits(items[index]);
+   if(current.sequenceHoldLoop)return remaining;
+   const configured=Math.max(1,current.sectionRepeatValue(section));
+   const remainingPasses=Math.max(0,configured-current.currentSectionRepetition);
+   return remaining+(remainingPasses*total);
+  }
+  function formatSequenceCountdown(units,state){
+   const current=state||snapshot();
+   const seconds=Math.max(0,Math.ceil((units*current.barDuration())/8000));
+   const minutes=Math.floor(seconds/60);
+   const remainder=seconds%60;
+   return String(minutes).padStart(2,'0')+':'+String(remainder).padStart(2,'0');
+  }
+  function syncCompactControls(){
+   const state=snapshot();
+   const capo=element('compact-capo-value');
+   const oct=element('compact-octave-value');
+   const loop=element('compact-loop');
+   const play=element('compact-play');
+   const sequenceOnly=element('compact-sequence-only');
+   const drum=element('compact-drum-only');
+   const current=element('compact-current-song');
+   const selectedSong=element('compact-selected-song');
+   if(capo)capo.textContent=state.capoSemitones>0?'+'+state.capoSemitones:String(state.capoSemitones);
+   if(oct)oct.textContent=state.octave>0?'+'+state.octave:String(state.octave);
+   if(loop){
+    loop.classList.toggle('active',state.sequenceHoldLoop);
+    loop.textContent=state.sequenceHoldLoop?'🔁 Sequência em loop':'🔁 Sequência atual';
+   }
+   const musicActive=state.sequencePlaying||state.sequenceStartQueued;
+   const drumsActive=state.drumRunning||state.drumStartQueued;
+   const anyTransportActive=musicActive||drumsActive;
+   if(play){
+    play.classList.toggle('active',anyTransportActive);
+    play.setAttribute('aria-pressed',String(anyTransportActive));
+    play.textContent=anyTransportActive?'■ Parar Música + Bateria':'▶ Música + Bateria';
+   }
+   if(sequenceOnly){
+    sequenceOnly.classList.toggle('active',musicActive);
+    sequenceOnly.setAttribute('aria-pressed',String(musicActive));
+    sequenceOnly.textContent=musicActive?'■ Parar Música':'🎼 Só Música';
+   }
+   if(drum){
+    drum.classList.toggle('active',drumsActive);
+    drum.setAttribute('aria-pressed',String(drumsActive));
+    drum.textContent=drumsActive?'■ Parar Bateria':'🥁 Só Bateria';
+   }
+   if(selectedSong)selectedSong.textContent=state.currentSongName||'Nenhuma música selecionada';
+   if(current){
+    current.textContent=state.currentSongName
+     ?state.sectionLabels[state.activeSequenceSection]+' · '+state.bpm+' BPM'
+     :'Nenhuma música carregada';
+   }
+   syncListControls();
+   renderCarousel();
+  }
+  function updateReadouts(){
+   const state=snapshot();
+   const songName=element('redesign-song-name');
+   const bpmTop=element('redesign-bpm-readout');
+   const bpmBottom=element('redesign-mixer-bpm');
+   const play=element('redesign-play');
+   const playLoop=element('redesign-play-loop');
+   const currentLoop=element('redesign-current-loop');
+   const statusTarget=element('redesign-status');
+   const voices=element('redesign-voices-volume');
+   const drums=element('redesign-drums-volume');
+   if(songName)songName.textContent=String(state.currentSongName||'Nenhuma música');
+   if(bpmTop)bpmTop.textContent=String(state.bpm);
+   if(bpmBottom)bpmBottom.textContent=String(state.bpm);
+   if(play){
+    play.textContent='▶';
+    play.classList.toggle('active',state.sequencePlaying&&state.sequenceAutoEnd&&!state.sequenceAuto);
+    play.setAttribute('aria-pressed',String(state.sequencePlaying&&state.sequenceAutoEnd&&!state.sequenceAuto));
+   }
+   if(playLoop){
+    playLoop.textContent='↻';
+    playLoop.classList.toggle('active',state.sequencePlaying&&state.sequenceAuto&&!state.sequenceAutoEnd);
+    playLoop.setAttribute('aria-pressed',String(state.sequencePlaying&&state.sequenceAuto&&!state.sequenceAutoEnd));
+   }
+   if(currentLoop){
+    currentLoop.textContent='🔁';
+    currentLoop.classList.toggle('active',state.sequenceHoldLoop);
+    currentLoop.setAttribute('aria-pressed',String(state.sequenceHoldLoop));
+    currentLoop.title=state.sequenceHoldLoop
+     ?'Liberar a sequência atual e continuar a progressão'
+     :'Manter somente a sequência atual em loop';
+   }
+   if(voices)voices.value=String(Math.round((state.keyboardVolume+state.chordVolume+state.bassVolume)/3));
+   if(drums)drums.value=String(state.drumVolume);
+   if(statusTarget)statusTarget.textContent=state.statusText;
+  }
+  function syncCompactSequenceCountdown(){
+   const state=snapshot();
+   const countdown=element('compact-seq-countdown');
+   const label=element('compact-seq-countdown-label');
+   const value=element('compact-seq-countdown-value');
+   if(!countdown||!label||!value)return;
+   const active=state.sequencePlaying||state.sequenceStartQueued;
+   countdown.hidden=!active;
+   if(!active)return;
+   label.textContent='Restante · '+(state.sectionLabels[state.activeSequenceSection]||('Sequência '+state.activeSequenceSection));
+   value.textContent=formatSequenceCountdown(sequenceRemainingUnits(state.activeSequenceSection,state),state);
+  }
+
+  return Object.freeze({
+   syncCompactControls:syncCompactControls,
+   updateReadouts:updateReadouts,
+   syncCompactSequenceCountdown:syncCompactSequenceCountdown,
+   sequenceItemUnits:sequenceItemUnits,
+   sequenceTotalUnits:sequenceTotalUnits,
+   sequenceRemainingUnits:sequenceRemainingUnits,
+   formatSequenceCountdown:formatSequenceCountdown
+  });
+ }
+
+ global.GeraTransportStatus=Object.freeze({createConsumer:createConsumer});
+})(window);
